.agents/
!.agents/skills/
public/
node_modules/
# slashless twin: worktree setups symlink node_modules as a FILE, which
# the dir-only pattern above never matches (design-studio worktree, 2026-09-11)
node_modules
# the jixoai design studio workspace (scaffolded; templates live in
# packages/design-tool/src/scaffold/templates — design-studio, 2026-09-11)
design/
.worktree/

# apps/www (ui.jixoai.com) build state
apps/www/node_modules/
apps/www/dist/
apps/www/.svelte-kit/

# blueprint incremental-build cache (derivable; SVGs are committed)
scripts/blueprints/.cache.json
.zcode/
pnpm-lock.yaml

# tw-context-probe fixture (generated at test time)
apps/www/.tw-probe-fixture/

# svelte-kit build dir wherever a tool drops it (observed: registry/ after a bun dev run)
**/.svelte-kit/

# dev syncer runtime droppings (recurring maintenance class — never commit)
registry/files/routes/
registry/mirror-manifest.json
registry/static/
registry/test/
registry/.tw-probe-fixture/
registry/.tw-standard-probe/

# tw4 standard probe scratch (generated, never commit — history-purged 2026-09-05)
apps/www/.tw-standard-probe/

# example build outputs (the hmos E2E builds in-tree when run by hand)
examples/*/dist/
examples/*/dist-ssr/
