// native-scroll-area — pure barrel (the family's D3 precedent):
// default = the canonical main; export * carries module-level named
// exports/types. No logic lives here.
export { default } from './native-scroll-area.svelte';
export * from './native-scroll-area.svelte';
