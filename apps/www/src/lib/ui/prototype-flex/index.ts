// prototype-flex — pure barrel (tabs precedent): default = the
// canonical main; export * carries module-level exports/types. No
// logic lives here.
export { default } from './prototype-flex.svelte';
export * from './prototype-flex.svelte';
