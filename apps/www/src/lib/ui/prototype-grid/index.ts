// prototype-grid — pure barrel (tabs precedent): default = the
// canonical main; export * carries module-level exports/types. No
// logic lives here.
export { default } from './prototype-grid.svelte';
export * from './prototype-grid.svelte';
