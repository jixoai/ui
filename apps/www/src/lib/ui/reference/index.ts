// reference — pure barrel: default = the Reference primitive (引);
// it consumes figure's frozen registry and never builds one of its own.
export { default } from './reference.svelte';
export * from './reference.svelte';
