// mermaid — pure barrel (tw4-css-modularization D3): default = the
// canonical main; export * carries the module-level named types
// (MermaidLabels). No logic lives here.
export { default } from './mermaid.svelte';
export * from './mermaid.svelte';
