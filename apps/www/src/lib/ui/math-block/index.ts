// math-block — pure barrel (the toc/inline-code precedent): default =
// the canonical main; export * carries the module-level named types
// (MathBlockLabels). No logic lives here.
export { default } from './math-block.svelte';
export * from './math-block.svelte';
