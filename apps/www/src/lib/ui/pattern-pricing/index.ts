// pattern-pricing — pure barrel (folder law): default = the canonical
// main; export * carries the PricingTier payload type. No logic lives
// here.
export { default } from './pattern-pricing.svelte';
export * from './pattern-pricing.svelte';
