// W3 progress probe route — prerendered as its own crawl entry (no
// inbound links; probe-popover-area precedent). Internal verification
// surface for research/w3/timeline-progress-probe.mjs; never indexed.
export const prerender = true;
