// the parity fixture page is a GATE surface (verify-native-parity.mjs),
// deliberately unlinked from navigation — adapter-static strict mode
// needs the explicit page-level prerender claim (crawl is off)
export const prerender = true;
