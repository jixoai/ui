# tasks — composition-first-apis

- [ ] 1. Audit + research + OpenSpec scaffold (this change)
- [ ] 2. Spec delta lands: component-authoring gains the
      composition-first requirement (review with owner)
- [ ] 3. Batch A (subagent): steps + timeline + descriptions families
      — registry + mirror + demo pages + tests
- [ ] 4. Batch B (subagent): breadcrumb + pagination (+ range helper
      tests) + anchor + terminal-footer
- [ ] 5. Batch C (subagent): menubar + navigation-menu (dropdown-menu
      shape) + toggle-group
- [ ] 6. Batch D (subagent): toc + hero-section + tour + alert-dialog
      + popconfirm
- [ ] 7. Batch E (subagent): command family (registration-based
      filtering, kb nav preserved)
- [ ] 8. Batch F (subagent): terminal-header decomposition + docs-site
      chrome migration (last — rides on C's menubar machinery)
- [ ] 9. ZCode integrator: registry.json entries per family, cross-batch
      fixes, per-batch commits
- [ ] 10. Gates: pnpm build + verify:mirror + apps/www vitest +
       verify-hook-law + demo walkthrough on dev server
- [ ] 11. Codex review round (Herdr, async) + iterate to green
- [ ] 12. Archive change; spec updated; exceptions table carried into
       component-authoring as the audit checklist
