# BOARD — current assignments
| agent | task | page | type | status |
|---|---|---|---|---|
| quill | 69 | toc (OWN) | CODE | in-flight |
| vellum | 75 | tabs (vellum's OWN — scribe 1sted) | REVIEW (2nd) | in-flight |
| marginalia | 74 | separator+spin RE-ADJUDICATION | PROBE | in-flight |
| scribe | 71 | toast (vellum's) | REVIEW (1st) | in-flight |

## DONE (54)
- ✅ **anchor** (scribe) — 1st · **blockquote** (quill) — 2nd · **alert** (vellum) — 3rd · **accordion** (marginalia) — 4th · **badge** (marginalia) — 5th (component EXTRA-lane rescue) · **chip** (quill) — 6th · **breadcrumb** (scribe) — 7th (LAW #14) · **date-picker** (quill) — 8th (one-cell theme-split) · **checkbox** (scribe) — 9th · **component-canvas** (quill) — 10th (EXTRA doubled) · **cascader** (quill) — 11th (tier-3 exemplar) · **empty** (quill) — 12th (frozen pole triple-level) · **dropdown-menu** (quill) — 13th (the OVERTURN arc; shape-agnostic grep law) · **heading** (quill) — 14th (inline-stamp mechanism) · **avatar** (vellum) — 15th (second overturn arc) · **button-group** (vellum) — 16th (PROVIDER; --jx-inset rode closure) · **inline-code** (quill) — 17th (pin-density tier-3; toc row rode closure) · **image** (quill) — 18th (snippet-vs-default MAJOR arc) · **color-picker** (scribe) — 19th (stamped-never-consumed; chain-head settled 24−8−4=12) · **hero-section** (quill) — 20th (THE ADJUDICATION; LAW #16) · **input-group** (quill) — 21st (structural-arm MEDIUM → fixed fleet-wide 27 pages) · **carousel** (marginalia) — 22nd (same-token opposite-emission demonstrator) · **descriptions** (scribe) — 23rd (rig-vs-law arc; the empirical flex-basis fix; 64rem boundary proof) · **link** (quill) — 24th (the frozen pole's pure-alias; em-voices consumption measured 11.1875px; marginalia 2nd PASS — order-receipt probe craft: byte positions, not membership; consolidation restored the api→axes→accessibility trio + install/see-also id anchors) · **code-card** (quill) — 25th (the FOURTH theme pattern dropped-own-adaptation; the two-medium :where() order-arbitration receipt 42/78/42-12; zero-span raw SSR vs 68-span workbench; scribe 2nd PASS — the −4° arithmetic pinned by both reviewers; id anchors landed; W-next #3 description-arm author-side-confirmed) · **native-scroll-area** (quill) — 26th (the observer-consumed FIFTH mechanism double-verified live — both observer channels, light→dark→light no re-mount, nearest-wins .jx-light shadowing; authored-9 + folded-8 = served-17, the fold documented in-page; the LAST-WRITE incident disclosure truthful against four records; zero [role=scrollbar] — platform bar only, 12/12 region+label+tabindex) · **card-grid** (vellum) — 27th (the tenant-swap fix DOUBLE-verified: SectionCard carries the density ladder live-true 8/12 xs/sm · 12/16 default · 12/20 lg with channel compositions; the grid's 20px gap inert at every rung; theme-bridge canvas three assertions TRUE — and marginalia's independence caught the drawer reusing the axes files, fixed with its own themeFiles const at closure; the swap law applies to ADDITIONS, not just rewrites) · **navigation-menu** (quill) — 28th (the image model; promotion-away radius channel double-verified on real open popovers 0→10px with 0.46s kernel transitions awaited; theme split read clean — no hue reads, the --muted-foreground/--popover complementary token pair reconciled across both reviewers; density declaration-scope freeze; the tabindex LOW independently confirmed by both reviewers and reworded to the plain-span truth; + the FLEET ID-ANCHOR SWEEP at closure: 40 wrappers across 22 closed pages — the class retired for everything not pending CODE) · **badge-indicator** (marginalia) — 29th (the chrome POSITIVE exemplar; posture-split size echo double-verified (standalone mirror 18px beats the 10px atom / riding = wrap carries / stamped-chip unchanged); the cleanest raw-vs-typed pair (raw flips 0→1, typed frozen, chip black/white regardless — alias declares var(--destructive) AT :root); PressButton 40→48 at THREE widths proving scope-driven; the corrected EXTRA form (14 served, the −1 IS class, no rest row) reproduced byte-for-byte by both reviewers — the falsification-law dividend; zero badge/chip imports) · **combobox** (vellum) — 30th (two self-fixed MAJORs held across five integrations; floor-asymmetry digit-exact incl. the shell 40/40/40/48 rendering; ARIA chain on REAL events; two-time-bases one-evaluate; the THIRD time base discharged BOTH ways — emulateMedia + the island probe proving the class channel never moves it; 21 rows by name; the wrap-IN-PLACE island law: body-appended islands sever tokenScope and fake refutations) · **ghostty-term** (vellum) — 31st (the wasm exemplar; the live misfold fixed via EXTRA; the SHADOWED SLOT double-blinded: quill derived boot-time-snapshot, scribe independently derived NO-OBSERVER with the refinement that prop-driven re-runs DO re-probe — the lock is on scope observation, not on time; wording refined at closure to carry both mechanisms; KEEP×3 concurred independently; density ladder consumed arithmetic; the nothing-follows proof on the fixed error face) · **file-input** (vellum) — 32nd (the fleet's deepest density ladder double-verified digit-exact; the drop-gate DOUBLE-BLIND ADJUDICATED by orchestrator on a live probe: NO LEAK — quill's 7 reproductions dropped on the UNGATED trigger (querySelector('button') grabs the DOM-first button inside the shared demo root; the gated trigger's aria-label carries the label prop "gate — accept: image/*"); gated-trigger drops reject verbatim — txt excluded from the value, error line rendered, describedby wired, mixed binds png only — marginalia's receipt confirmed; craft law: anchor drop probes by aria-label/label context, never positional querySelector; id prop row + id anchors landed at closure) · **kbd** (vellum) — 34th (the NEEDS-WORK arc: quill's vocabulary-grep caught the falsified caption surviving in paraphrase on two surfaces — the 17th law; fixes landed by orchestrator, scribe re-verified clean; typed-REDECLARED flip digit-verified with the −4° drift live; radius 2px stamped-but-ignored across carrier demos) · **popconfirm** (quill) — 35th (the dead-trigger page; marginalia's self-continuation held the reproduce-don't-trust standard with a fresh drift audit; the override panel's dialog+labelledby-null drop claim now probe-verified; composition-d adjudication stands; W-next #5 owns the family clobber) · **menubar** (vellum) — 36th (the in-place third shape; glide one-hop on REAL clicks + the Enter-path proof of the trigger-focus precondition; radius three-state chain; split-voice with the frozen light OPEN band — W-next #7 CONFIRMED by scribe with the source line (menubar.stylex.ts:58-59 atom freeze vs menubar.css hover raw chain); the bar-gap flip-limit note landed in a LIVE seat after the TokenTable-description dead-API discovery (W-next #3 second instance); the five-rungs count-word nit recorded) · **number-input** (vellum) — 37th (the deepest stepper page closed; scribe's 2nd found the seam's FIRST HARD LEGIBILITY FAILURE — the dark field's value invisible at 1:1 (cell ink re-derives on the frozen white shell; page clause corrected + cost disclosed, family half on W-next #7); api summary trued (21/7 + fold note); dense specimen re-runged; family comments fixed MIRRORED — drift #11's first fix, both copies byte-identical) · **press-button** (vellum) — 39th (the physics-and-paint ladder page closed on a verified closure: marginalia's 1st three findings all landed 80720a6a and scribe's 2nd re-verified each — the 42px drift mirrored byte-identical across all 7 family files with the ?raw module endpoint serving the fixed text verbatim; the hue caveat EMPIRICALLY proven by the wall-clock's third sample (129→125→137, L/C 0.7044/0.1872 digit-exact every time — the signature form is the only reproducible receipt); the fill-label-ink freeze reproduced element-for-element; W-next #5's explicit-prop teaching re-proven live (forwarded attribute → opens → toggles, zero listeners); press ladder + hit ladder 24/28/32/40/48 + radius wiring + forced-colors LinkText all reproduced; residue INFO: family typing debt :451)) · **sheet** (quill) — 40th (the 4-side drawer closed on the campaign's deepest three-receipt instrument arc: the WHEEL SAGA — quill's chain receipt (0→1396), marginalia's pinned scroller, scribe's GENUINE-BOUNDARY test (scrollable cell scrolled to max, wheeled past — shell did not move) settled it: wheel is swallowed at EVERY hit target while the sheet is open, the scroller never locked (programmatic scroll works, nothing authors a lock) — the closure clause carries the settled truth and names no unverified mechanism; entry pixels digit-exact through every waypoint settled ~182ms, Escape exit 225ms with focus restore on a REAL click (the programmatic-click artifact ledgered by two reviewers), RM double-kill 19ms; the LAW #19 pre/post exhibit page — 6 twins pre-guard, ZERO post, adoption signature visible; W-next #8 workaround live (384 @ x896, 18rem→288 @ x992), defect receipt historical; scribe's NIT recorded: the level4 shadow paints on the card kernel's surface box — the next prober reads the right element)) · **prototype-flex** (vellum) — 42nd (the trio's lead closed on TWO independent self-proving fixtures: scribe's ComponentProps path and marginalia's mount()-props path both proved the query boundary — gap rejects QueryResult non-vacuously, size accepts; the rig verified byte-identical both directions twice (the attribute reverts to the CSSOM-merged flex-flow shorthand — equivalent form, computed truth byte-identical); marginalia's LOW landed at closure with git-resolved provenance: the rig-usage predicate matched rig.svelte but the file is prototype-flex-usage.svelte — the dock's live mirror was DEAD since integration, falsifying the coder's mirrors-the-rig receipt (no served prose repeated the claim — the page carried no false statement); predicate fixed; the dock-vs-drawer NIT word fixed; the x-positions receipt 1050/694/330 for the row-reverse trade)) · **scroll-virtual** (quill) — 43rd (closed with a RECORD CORRECTION: the 1st review's '110 wrappers @100k' superseded by three agreeing sources — scribe's census, the page's own PlayFields readout, quill's original claim — the true count is 14 at rest / 20 post-jump, count-independent windowing at the 288px stage; the orchestrator's baseline fetch re-verified in the 2nd's own run (served toc == data.toc, 947,074-byte payload, 8 ids ×2 rails); spacer raw style 'block-size: 4e+06px' = exactly 4,000,000px; the forwarding stamps walk-up reproduced; reserved-key precedence source-pinned; LAW #18/#19 clean; quill's OQ1 resolved by the LAW #19 post-guard, OQ2 the radius-seam silent-drop stays a fair meta-annotation)) · **prototype-waterfall** (vellum) — 44th (closed through the campaign's cleanest NEEDS-WORK arc: scribe's 1st falsified the rem-nuance with the two-direction experiment; the five-seat correction landed — and marginalia's SSR old-claim census caught the MISSED SIXTH SEAT (the query-panel prose still teaching the falsified mechanism verbatim, live-falsifiable by the page's own resize invitation); the one-sentence fix + the dormant predicate (the flex sibling — rig.svelte vs the usage filename) repaired BOTH the dock mirror and the PlayHelp's claim; re-verify PASS with the census reading zero, the mirror receipted live after a real protocol drive (selectOption/fill — her instrument honesty: manual synthetic events silently no-op'd); the settle phrasing quantified twice (16→24 stale/settled, then 24→30 on the current tree))) · **prototype-grid** (vellum) — 45th (THE TRIO COMPLETES; closed on zero new findings with concordance to the THIRD DECIMAL between reviewers — 240.328/180.25-360.5/145.25×4/228.328/45.5/66 ids all reproducing; the no-blowout injection re-proven by RECT GEOMETRY (chip right 1435 vs grid right 1075, removal restores); the auto-fit re-count ladder receipted as a MECHANISM (4×145.25 → 3×149.7 → 2×160.5, the 140px floor never breached); the 12-flip control stress at LAW #18's birthplace — zero key dupes, zero console noise; the scrollWidth-is-unreliable lesson filed (rect geometry instead); the width-naming clause verified at its named seats — the carried LOW settled)) · **stack** (quill) — 46th (closed on zero new findings with the mutation envelope reproduced EXACTLY — root 16→32px moves the gap 8→16px and back, the 18px stamped voice never moves; the source chain receipted end-to-end: gap atom → var(--space-N) → calc(var(--jx-unit) × N/4) with --jx-unit: 0.25rem — rem-of-document-root, the px stamp outside the channel; the bare stack's attribute census exactly one stylex hash + the hook; scribe's record note: the rendered eaches are UNKEYED static literals — the 1st review's 'keyed' label wrong, the safety conclusion unchanged)) · **scroll-area** (vellum) — 47th (the structural archetype closed with the frozen-ink negative verified AT THE PAINT — the element's computed backgroundColor, both directions, per the var-vs-paint law born the same hour; the stamp topology receipted on the full 9-CELL radius × width matrix (every cell's style attr: null ambient / the owned-name double-stamp / the infinity capsule); the thumb contract at invariant form (0.18 == 0.18, ratios paired, aria-valuenow 0→61→100); the pins timeline on the family holder; toc 14/14 == DOM; LAW #18 60/[12,16]; the family MINOR → W-next #12: nextViewportId drifts across SSR requests — warm-reload byte-identity fails, the comment's 'SSR unique + stable' half-true, fix shape $props.id(); her URGENT CROSS-TASK FLAG caught the orchestrator's separator 500 (unescaped apostrophe in the closure correction — fixed c4ef8428, smoke 5×200, ambient 284/284))) · **system-dialog** (quill) — 48th (closed through the campaign's longest defect arc: the dead anchored rise receipted at CODE → the one-char fix SSR-crashed (LAW #20 born) → the flip repaired and measured live → marginalia's 1st trued the falsified width token row → scribe's 2nd verified BOTH landed halves at the paint and raised the width split to FULL CENSUS level (the whole served CSSOM walked: zero max-width rules, zero 24rem — the atom never ships; W-next #8's second repro pinned at the deepest level); the mechanics battery reproduced on real clicks/keys; the imperative trio's full resolution matrix at dead-center with zero leftover hosts; stamps + --jx-corner published + own elevation level3; the matrix re-pin ordinals verified against served table order; var-vs-paint applied — his receipts name the read site)) · **progress** (vellum) — 49th (closed through the campaign's signature instrument arc: inert-fill falsified by the 1st reviewer's burst pixels → the coder owned it and re-measured → the falsifier re-verified → the 2nd reviewer CONFIRMED RM persistence on a THIRD instrument ('primed to falsify; it survived') with a NAMED settle criterion; the discriminator's SLOPE proven the invariant (147-148px/s vs theoretical 149.3; her 2.25s span arithmetically exact — the playground overlay covers 112px of the 448px bar); the instrument post-mortem rejected THREE instruments on the record (CDP screencast, virtual time, her own zlib decoder — caught by cross-decoding via sips→BMP), browser-side decode the working answer with sibling-churn armor)) · **spin** (vellum) — 50th (THE ADJUDICATION PAGE: marginalia's 2nd settled the fleet question with an A/B in one session — the spin glyph's computed color AND the separator fill both HOLD at the element under html.dark while the html-level aliases re-derive; FLEET-WIDE FROZEN for the root-pinned-alias class, no carve-out; scribe's earlier FLIP receipt named an html-level var read, the TWO-READ PROTOCOL now the standing instrument; the tags-input chip flip reconciled as the SLOT-BLOCK class per W-next #2's declaration-coverage cut — no contradiction, two classes; five seats corrected at closure (smoke 3×200); drift #14 gained its SSR face (7 lines of hash drift per request); the SMIL channel verified at the component's own API (animationsPaused on all 16 instances + her clock 0.62/0.00/0.6); the negative-delay ladder −800→−720→−640 (800ms ÷ 10); drift #12 mirrors byte-identical)) · **separator** (vellum) — 51st (closed by its own falsification arc: the root-dark half falsified at the paint by the 1st reviewer → three seats corrected (the orchestrator's apostrophe 500 caught by the cross-task flag and repaired en route) → the 2nd re-verified all three seats CONSISTENT (the axis row teaches the solid variant's token path; the overview/theming rows teach the served ghost's physics holds — not a contradiction, two surfaces) and re-confirmed the HOLD anchor via the two-read protocol; the FIFTH fleet data point banked: system-dialog FLIP on --jx-foreground, the same chain as tabs — the mechanism's second confirmation; the ghost law's seven-sample delta-0 spot-checked across base/island/root-dark; census 117/116/113 reconciled as commit-time drift — served-row counts quote at-read-time; 74 ids zero dups)) · **table** (quill) — 52nd (the surgical rebuild closed on a digit-exact three-lane radius re-derivation + the frame-width law both ends + the a11y battery on real clicks (the tri-state via the BUTTON, the indeterminate selection rule, caption census 20/20); the family radius fix's full confirmation; W-next #10 STRENGTHENED by the NIT (dense is a visual no-op at EVERY rung — gap==inset 8px sm / 16px lg, row height 34px either way; the clause now says so); W-next #11 gained its AT COST (both PlayRows' aria-labelledby resolve the first label — the page-size control announces 'frame width'; not cosmetic); the tokens classification receipt (the --jx-table-* locals ride the color-surface class, the theme bridge re-voices through the scope — the two classes measured apart); the SSR tasks-columns echo recorded)) · **tags-input** (vellum) — 53rd (closed on the ADJUDICATION OVERTURN: the pin-finder census — 22 chips under root dark splits 14 FLIP / 8 HOLD, every HOLD pinned by a demo-stage data-theme=light; the declaring rule ':root, .xbpgcew { --jx-muted: var(--muted) }' is LIVE — the THREE-TIER MODEL lands: root dark flips alias consumers, mid-tree .dark islands cannot, data-theme stages locally pin; BOTH earlier stories retire; marginalia's own first falsify retracted in-report with the full arc; real-clipboard paste used, the synthetic ClipboardEvent silently no-ops)) · **boot-splash** (marginalia) — 54th (THE OLDEST CODED PAGE closed (task 34) through a three-reviewer arc: the dead-workbench fix, the clock receipts, the two closure clauses landed and verified on both paths (manual+RM 0ms instant vs auto+RM keeping the 350ms floor as the blink-flash guard; manual uncapped past a 4s cap, the cap never arms); the LAW #16 theme pair digit-exact in THREE voices (prop-alone paints light, ancestor .dark flips, emulated prefers-color-scheme flips the same pair); the meta arithmetic bit-exact at the IR (21 = 13 family + 8 axes, agreed by two independent counting paths); the head block ships while closed with the noscript raw-bytes distinction banked; vellum's additions: the DOGFOOD PROBE-TIMING CAPTOR (the scaffold SSR-ships its own splash that self-dismisses ~1s — early probes race it, two passes disagreed before the captor was named) and the toc rail double-render dedup; her self-caught drafting-in-anticipation rewrite recorded — the independence discipline applied to her own cross-check; W-next #9 the family-spec absence stands as the ledger residue) · **skeleton** (quill) — 41st (the zero-vocabulary family closed on THREE-WAY CONCORDANCE — three instruments converging on the same pulse envelope (quill 120 samples/2s, marginalia 100/1.65s, scribe 91/1.5s): opacity 0.45→1.0 on jx-skeleton-pulse 1.4s, brightness not shimmer, the no-gradient negative triple-checked; RM freeze, merge law consumer-wins, never-manufactures to the pixel (128×12 / 40×40); the dead-utility re-host verified by live-DOM query — zero elements on ten retired selectors against 40 live blocks; scribe's 2nd filed ZERO findings)

## Pipeline (open work)
- reference: coded Tier-2 (quill 28 — THE INHERITANCE POLE: kbd's contrast measured in two contexts (13.5px pure inheritance / 18px stamped); density STAMP-ONLY; theme bridge-only with NOTHING reading it (cascade-passive by contract); the numbered-domain rig discovery (Figure referenceable only inside a numbered SectionCard — the old universal demo had the latent flaw); SSR edge claims 5 prerendered) → reviews QUEUED
- transfer: coded Tier-2 (vellum 46, coder label Tier B — **HER CODE QUEUE COMPLETE**; LAW #18's double weight measured: the panels render COMPLEMENTS of one option set (source ∩ target = empty, measured) so keys are unique across the UNION, moves are ONE atomic assignment — no transit state where a key exists twice; the mounted census matched the value list after every move class (+1 single / +2 batch to the empty-'no matches' state / −3 return / restored); selection-cleared post-move, legends as footers of truth, movers disabled at zero movable, search 1/3 visible, disabled deno never crosses, FormData bridge echoes value=keep; **THE PAGE-LEVEL CAPTURE find**: every served transfer sits in a canvas stage whose data-theme=light pin holds --card light — under root dark --jx-card re-derives 1.0→0.3211 at :root while the pinned panel holds, BOTH measured in one render; the three-reading hunt (token at root → token at panel → pin at the stage ancestor) recorded so nobody repeats it; seven strata named per claim; density the ONE consumed axis (12px sm rows vs 13px ambient); THREE FAMILY FLAGS: a role=status legend candidate for move announcements (flagged not fixed), a dev-mode duplicate-value warning candidate, root-dark demos must mount outside the stage) → reviews QUEUED
- textarea: coded Tier-2 (quill 68 — **THE EXTRA TAXONOMY'S SHARPEST FORM — three verdict classes, by name, measured**: forwarded WITH A JOB (maxlength drives the count readout, maxLength=10 read back; rows default 4 verbatim; oninput fires AFTER the family syncs), forwarded VERBATIM (spellcheck false, wrap "hard", name — all read off the element), WITHHELD (the color ATTRIBUTE omitted — the color AXIS not; caller data-density captured by the axis stamp) — attribute-transparent, not rest-less, the anti-progress; the §1 collision rule measured (size-stamped fields carry NO size attribute); the ANNOUNCEMENT DISCIPLINE both channels (the count readout counts code points (🫠🫠 = 2), aria-live OFF → polite at ≥90% of cap → back to OFF asserted both directions; the error line a STATIC describedby target — no typing chatter); THEME STRATA named per claim (L1 page bridge / L2 host island — the canvas stage re-pins, the served demo does not re-derive / L3 component stamp — closest scope); DENSITY: no-opinion AND reactive (10px/60px at 2xs vs 15px/90px at lg — terminal-footer's sharp contrast); the stub corrected (the border is the monochrome signal, the message line paints var(--destructive)); the {...rest}-as-prose brace trap caught by the SSR canary — the two-strike pattern) → reviews QUEUED
- tour: coded Tier-2 (vellum 45, coder label Tier B — the FOCUS LAW measured on real clicks (open lands Next, every step re-lands, Escape/Skip restore the invoker — programmatic clicks never focus, the probe fault owned); the REVERSIBLE anchor-name lease verified verbatim on every transition (--jx-tour-s25 per-instance, prior restored, dataset cleaned); placement bottom 12px exact + composed overrides; scrollIntoView 0→236; step-move 1-frame settle ×3 (pure CSS anchor repositioning); **the {#if} reopen gap DETERMINISTIC** (fresh-page first open tweens --jx-p 0.109→0.652 over the 460ms kernel; every reopen snaps flat 1.000 — the family's recorded gap quantified, not a race); dismissal ×3 (Skip/Escape/Finish + onfinish receipt); theme split: the dark-island card re-derives ground (level4 0.245) with FROZEN black ink at 3.54:1 (the seam's tour flavor); **radius DRIFT confirmed → drift #15**: --jx-radius-consumed stamped, computed 0px, no reader in the chain (owner's call: missing rule vs vestigial); dots keyed; 90 ids zero dups; five probe faults owned) → reviews QUEUED
- terminal-footer: coded Tier-2 (quill 67 — **THE HOST LAYER find: theme-split LOCATION BEATS THE PROP** — the canvas stage pins data-theme="light" + .jx-light, re-pinning the light profile: page --border flips white while the served demo footer stays black-stroked, the SAME footer cloned to body re-derives white, a component .dark stamp re-derives in place; inside the component the stroke re-derives while --jx-muted-foreground ink freezes — the sixth mechanism refined into a PRECEDENCE RULE (closest themed ancestor > page theme; the prop wins only within its own scope)); ghost clamp measured (140.8px @1280 / 144 @1600 — the cap fires); shell 90rem centered with padding seams 16/24/32; the hover seam var(--motion-150) governs (2s under injection) and is RM-safe (0.15s preserved); size = inherited echo (free text 30px, ghost immovable); EXTRA nuance: ATTRIBUTE-TRANSPARENT not rest-less (rest forwards onto the real footer/column elements; the color ATTRIBUTE withheld, the color AXIS not); mirrors cmp 6/6; one authoring fix PlayToggle value-bind) → reviews QUEUED
- toast: coded Tier-2 (vellum 44, coder label Tier B — SELF-CARRIED PORTAL (all stacks in jx-top-layer/jx-float-slot); per-item live regions measured ([status×4] burst, sticky assertive → exactly 1 role=alert, content present AT insertion, +N chip aria-hidden); the timing battery with the instrument named: auto-dismiss 5224/5225/5227ms, exit frame 230ms (EXIT_MS 220 + frame, the .jx-toast-leaving ghost present-then-gone), RM 2ms dismiss / 5004ms auto, hover hold + 4222ms post-release, hidden-tab hold + 4962ms after resume (= remaining 4700 + 220 ghost — the arithmetic exact); FIFO promote-on-visibility (4 visible + 1 queued → front dismiss promotes the oldest); keys monotonic 11/12/13 distinct; **BOTH THEME MECHANISMS IN ONE CARD** (root-dark re-derives popover 1.0→0.3211 + surface 0.94→0.205 with a mid-tween 0.482 measured; dark island under light root re-derives ground+tonal while the popover aliases hold frozen white); **THE SECOND AA FAILURE → W-next #7 seventh voice: outline ink 2.81:1 on a dark island** (frozen :root ink over re-derived ground — family fix: stamp the ink pair under class:dark or re-declare in the theme scope); density 13→15→12px with attr flip; swipe 70px-fast dismisses / 30px-slow springs; six probe faults owned incl. the contrast helper's oklch-as-sRGB parser bug (1.64 garbage → 4.57 real); one pre-existing cx diagnostic hardened) → reviews QUEUED
- tabs: coded Tier-2 (vellum 42 — the ARIA chain on REAL keys: ArrowRight moves focus AND flips aria-selected (exactly one true per tablist) AND the roving stop follows; End with a disabled last trigger STAYS (the APG skip); inactive panels hidden + content UNMOUNTED (element-presence receipt), aria-labelledby deterministic; 29 tablists censused; the indicator's transition-frame: 9 distinct mid-travel rAF positions over the authored 240ms, RM 0s + single position (the bar jumps); density = the ONE consumed axis (kernel rescale in the trigger's hit height 48/32px); **THE SIXTH THEMING MECHANISM DISCOVERED: canvas stages pin data-theme="light"** — in-page demos stay on their authored ground by ancestor SCOPE (0.3211 inside vs 0.8452 above, same document; joins raw-flip/typed-frozen/bridge-only/dropped-own-adaptation/observer-consumed — the split vocabulary grows); the root-pinned alias measured on a THIRD family (--foreground held under scoped .dark, re-derived at root dark); two nonexistent rt atoms fixed (pre-existing HEAD type errors); the old toc missed seven real sections (now 13 == DOM); PROCESS INCIDENT owned: open(p,'w') truncated the page to zero bytes mid-task — recovered via git checkout + a scripted replay of nine edits with read-only assertions, which then caught a swallowed api section before anything shipped) → scribe 1st **PASS** (0M/0m/0L/0N — ZERO FINDINGS; the ARIA chain censused at page scale: 29 tablists / 129 tabs / 22 panels, End-skip on the disabled trigger, roving stop followed, panels UNMOUNTED by element-presence; the indicator 15-of-20 distinct rAF samples at 0.24s×3, RM 1-of-17 with 0s; density 48→32 digit-exact; **THE FOURTH ADJUDICATION DATA POINT: tabs FLIPS at the paint WITH the mechanism named** — --jx-foreground at :root, :root IS html, html.dark re-declares --foreground, the alias re-derives at root and inherits; FLEET LINE all paint-true: separator HOLD / tags-input FLIP / tabs FLIP / spin HOLD — chain-specific; the canvas-pinned ground under root dark (oklab 0.981142 inside); both rt-atom fixes verified; 241 ids zero dups) → 2nd QUEUED
- prototype-waterfall: coded Tier-2 (vellum 37 — the trio closes: the see-also LOOP implemented page-side (flex+grid gained cross-reference pills); the "14rem" FLOOR form measured end-to-end (column-width 224px, 3 columns @1400 → 2 @760 — width floored, count follows); the SIZE-STAMP REM NUANCE: rem-based column floors re-read the root stamp while count-form columns are scale-blind; LAW #18 receipts on all keyed eaches + mounted-children asserted on 8 roots; no JS in the family — control changes settle in one frame; the multicol doctrine caveat (short-card decks only) taught on-page) · **reference** (quill) — 38th (the inheritance pole closed; the unnumbered specimen MOVED domain-less with the join-rule note; the SSR census trued: 5 fallbacks + 6 resolved-label anchors, pre-settle not blind; the warm-reload law banked: cold visits read never-hydrated; reference CLOSED) 
## RULINGS: a11y floor (24 = WCAG 2.5.8 AA minimum; 28 = fleet default exceeding) · toc/See-also: chrome, OUT of the toc · component fixes: three-strikes → fix now (EXTRA lane, hardening pins, structural arm precedents) · reviewer page-edits: flag-only, route through owner/orchestrator

## Law library (20): SSR ground truth · consumed-vs-supply · canvas-id swap · declaring-element · measurement-first · composed-consumer · resolved-record · query() both-args · grep-receipt (SHAPE-AGNOSTIC) · disagreement-probe · EXTRA-lane (reference-identity, spec-pinned) · single-evaluate probes · TRANSITION-FRAME · PROBE-READINESS · FIRST-PAINT-BASE (§9.1: raw SSR serves the base; hydrated probes for flip claims; name your medium) · THEME-SPLIT (five mechanisms: raw-flip / typed-frozen / bridge-only / dropped-own-adaptation / observer-consumed; the typed-layer cut is DECLARATION COVERAGE: typed-REDECLARED-per-scope re-derives vs typed-ROOT-ONCE freezes — quill's kbd adjudication) · vocabulary-grep (LAW #17: a falsified claim survives in paraphrase — sweep the claim's VOCABULARY, not the exact string; quill's kbd catch, two surfaces) · KEYED-EACH UNIQUENESS (LAW #18: duplicate each keys abort hydration page-wide — SSR perfect, svelte-check blind; uniqueness keys + mounted-children probe; vellum's prototype-grid incident, caught again pre-integration on sheet) · WRAPPER-TWIN (LAW #19: the ToC stamper owns heading slugs — a heading whose nearest id-bearing ancestor already owns EXACTLY that slug adopts it unstamped; never mint a twin id; anchor law v2.1, corpus convergence locked 203 files/1185 sections @2b4b1002) · SSR-SMOKE BEFORE COMMIT (LAW #20: family-file edits touching render-time code need a served-page 200 receipt before integration — svelte-check's warning classes can mask fatal init-order crashes; the anchor TDZ shipped as '+2 benign warnings' and served HTTP 500, caught only by quill's live probe; orchestrator's own measurement-first law applied to itself)
## Defect classes: measured-must-reproduce · rig-must-perform-its-claim · floor-asymmetry · law-true-rig-broken
## Ledgers: drift 11 (BOTH catches FIXED — number-input closure #37 + press-button 80720a6a; the class rule stands: vocabulary sweeps include family comments when the drawer is same-source) · W-next 9 (7 open: #1 semantic-ink gap · #2 theme-split refinement · #3 TokenTable description arm (sourceLabel half FIXED) · #4 dev CSSOM transient · #7 frozen-ink seam (5 voices) · #8 canvas-host width atom loss · #9 boot-splash family spec absence; 2 settled: #5 CLOSED-narrowed, #6 FIXED family-side)
