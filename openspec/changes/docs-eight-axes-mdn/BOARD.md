# BOARD — current assignments
| agent | task | page | type | status |
|---|---|---|---|---|
| vellum | 36 | prototype-grid (OWN) | CODE | in-flight |
| quill | 31 | sheet (OWN) | CODE | in-flight |
| scribe | 35 | reference (quill's) | REVIEW (2nd) | in-flight |
| vellum | 37 | prototype-waterfall (OWN) | CODE | in-flight |
| marginalia | 37 | reference (quill's) | REVIEW (1st) | in-flight |

## DONE (37)
- ✅ **anchor** (scribe) — 1st · **blockquote** (quill) — 2nd · **alert** (vellum) — 3rd · **accordion** (marginalia) — 4th · **badge** (marginalia) — 5th (component EXTRA-lane rescue) · **chip** (quill) — 6th · **breadcrumb** (scribe) — 7th (LAW #14) · **date-picker** (quill) — 8th (one-cell theme-split) · **checkbox** (scribe) — 9th · **component-canvas** (quill) — 10th (EXTRA doubled) · **cascader** (quill) — 11th (tier-3 exemplar) · **empty** (quill) — 12th (frozen pole triple-level) · **dropdown-menu** (quill) — 13th (the OVERTURN arc; shape-agnostic grep law) · **heading** (quill) — 14th (inline-stamp mechanism) · **avatar** (vellum) — 15th (second overturn arc) · **button-group** (vellum) — 16th (PROVIDER; --jx-inset rode closure) · **inline-code** (quill) — 17th (pin-density tier-3; toc row rode closure) · **image** (quill) — 18th (snippet-vs-default MAJOR arc) · **color-picker** (scribe) — 19th (stamped-never-consumed; chain-head settled 24−8−4=12) · **hero-section** (quill) — 20th (THE ADJUDICATION; LAW #16) · **input-group** (quill) — 21st (structural-arm MEDIUM → fixed fleet-wide 27 pages) · **carousel** (marginalia) — 22nd (same-token opposite-emission demonstrator) · **descriptions** (scribe) — 23rd (rig-vs-law arc; the empirical flex-basis fix; 64rem boundary proof) · **link** (quill) — 24th (the frozen pole's pure-alias; em-voices consumption measured 11.1875px; marginalia 2nd PASS — order-receipt probe craft: byte positions, not membership; consolidation restored the api→axes→accessibility trio + install/see-also id anchors) · **code-card** (quill) — 25th (the FOURTH theme pattern dropped-own-adaptation; the two-medium :where() order-arbitration receipt 42/78/42-12; zero-span raw SSR vs 68-span workbench; scribe 2nd PASS — the −4° arithmetic pinned by both reviewers; id anchors landed; W-next #3 description-arm author-side-confirmed) · **native-scroll-area** (quill) — 26th (the observer-consumed FIFTH mechanism double-verified live — both observer channels, light→dark→light no re-mount, nearest-wins .jx-light shadowing; authored-9 + folded-8 = served-17, the fold documented in-page; the LAST-WRITE incident disclosure truthful against four records; zero [role=scrollbar] — platform bar only, 12/12 region+label+tabindex) · **card-grid** (vellum) — 27th (the tenant-swap fix DOUBLE-verified: SectionCard carries the density ladder live-true 8/12 xs/sm · 12/16 default · 12/20 lg with channel compositions; the grid's 20px gap inert at every rung; theme-bridge canvas three assertions TRUE — and marginalia's independence caught the drawer reusing the axes files, fixed with its own themeFiles const at closure; the swap law applies to ADDITIONS, not just rewrites) · **navigation-menu** (quill) — 28th (the image model; promotion-away radius channel double-verified on real open popovers 0→10px with 0.46s kernel transitions awaited; theme split read clean — no hue reads, the --muted-foreground/--popover complementary token pair reconciled across both reviewers; density declaration-scope freeze; the tabindex LOW independently confirmed by both reviewers and reworded to the plain-span truth; + the FLEET ID-ANCHOR SWEEP at closure: 40 wrappers across 22 closed pages — the class retired for everything not pending CODE) · **badge-indicator** (marginalia) — 29th (the chrome POSITIVE exemplar; posture-split size echo double-verified (standalone mirror 18px beats the 10px atom / riding = wrap carries / stamped-chip unchanged); the cleanest raw-vs-typed pair (raw flips 0→1, typed frozen, chip black/white regardless — alias declares var(--destructive) AT :root); PressButton 40→48 at THREE widths proving scope-driven; the corrected EXTRA form (14 served, the −1 IS class, no rest row) reproduced byte-for-byte by both reviewers — the falsification-law dividend; zero badge/chip imports) · **combobox** (vellum) — 30th (two self-fixed MAJORs held across five integrations; floor-asymmetry digit-exact incl. the shell 40/40/40/48 rendering; ARIA chain on REAL events; two-time-bases one-evaluate; the THIRD time base discharged BOTH ways — emulateMedia + the island probe proving the class channel never moves it; 21 rows by name; the wrap-IN-PLACE island law: body-appended islands sever tokenScope and fake refutations) · **ghostty-term** (vellum) — 31st (the wasm exemplar; the live misfold fixed via EXTRA; the SHADOWED SLOT double-blinded: quill derived boot-time-snapshot, scribe independently derived NO-OBSERVER with the refinement that prop-driven re-runs DO re-probe — the lock is on scope observation, not on time; wording refined at closure to carry both mechanisms; KEEP×3 concurred independently; density ladder consumed arithmetic; the nothing-follows proof on the fixed error face) · **file-input** (vellum) — 32nd (the fleet's deepest density ladder double-verified digit-exact; the drop-gate DOUBLE-BLIND ADJUDICATED by orchestrator on a live probe: NO LEAK — quill's 7 reproductions dropped on the UNGATED trigger (querySelector('button') grabs the DOM-first button inside the shared demo root; the gated trigger's aria-label carries the label prop "gate — accept: image/*"); gated-trigger drops reject verbatim — txt excluded from the value, error line rendered, describedby wired, mixed binds png only — marginalia's receipt confirmed; craft law: anchor drop probes by aria-label/label context, never positional querySelector; id prop row + id anchors landed at closure) · **kbd** (vellum) — 34th (the NEEDS-WORK arc: quill's vocabulary-grep caught the falsified caption surviving in paraphrase on two surfaces — the 17th law; fixes landed by orchestrator, scribe re-verified clean; typed-REDECLARED flip digit-verified with the −4° drift live; radius 2px stamped-but-ignored across carrier demos) · **popconfirm** (quill) — 35th (the dead-trigger page; marginalia's self-continuation held the reproduce-don't-trust standard with a fresh drift audit; the override panel's dialog+labelledby-null drop claim now probe-verified; composition-d adjudication stands; W-next #5 owns the family clobber) · **menubar** (vellum) — 36th (the in-place third shape; glide one-hop on REAL clicks + the Enter-path proof of the trigger-focus precondition; radius three-state chain; split-voice with the frozen light OPEN band — W-next #7 CONFIRMED by scribe with the source line (menubar.stylex.ts:58-59 atom freeze vs menubar.css hover raw chain); the bar-gap flip-limit note landed in a LIVE seat after the TokenTable-description dead-API discovery (W-next #3 second instance); the five-rungs count-word nit recorded) · **number-input** (vellum) — 37th (the deepest stepper page closed; scribe's 2nd found the seam's FIRST HARD LEGIBILITY FAILURE — the dark field's value invisible at 1:1 (cell ink re-derives on the frozen white shell; page clause corrected + cost disclosed, family half on W-next #7); api summary trued (21/7 + fold note); dense specimen re-runged; family comments fixed MIRRORED — drift #11's first fix, both copies byte-identical)

## Pipeline (open work)
- boot-splash: coded Tier-2 (marginalia 34 — the DEAD WORKBENCH fixed (rendered instance + bind-back, replay proven); clock receipts 797/454/531/430ms per mode; controlled theme pair with LAW #16 named on both; 3 open questions: manual no-cap api row, instant-under-RM family question, animationend latency quoting) → reviews QUEUED
- reference: coded Tier-2 (quill 28 — THE INHERITANCE POLE: kbd's contrast measured in two contexts (13.5px pure inheritance / 18px stamped); density STAMP-ONLY; theme bridge-only with NOTHING reading it (cascade-passive by contract); the numbered-domain rig discovery (Figure referenceable only inside a numbered SectionCard — the old universal demo had the latent flaw); SSR edge claims 5 prerendered) → reviews QUEUED
- press-button: coded Tier-2 (vellum 33 — Step 0 re-trued number-input's error copy against the FIXED family (dashed+destructive live); DUPLICATE id="usage" deduped (invalid DOM); 3 stale external-law spots corrected; press ladder rest-xs→hover-sm→active-1px measured; hit ladder 24/28/32/40/48 with 2xs=WCAG minimum live; radius wiring (dock 20−14→6 / medium 8 / 10+squircle superellipse(2)); theme split WITHIN the ladder (fill re-tints, outline frame flips, outline/ghost label ink FROZEN — the frozen-ink seam SECOND instance, menubar's triggerOpen was first); forced-colors LinkText emulated-verified (the law's citation); W-next #5 truth update: the EXPLICIT popovertarget path is live (opens+toggles, zero listeners) — the AUTO-WIRE clobber (unset prop) stands as quill measured, #5 KEPT pending reviewer adjudication; 4 open questions) → marginalia 1st **PASS** (pixel-exact ladder with engagement receipts incl. counter-shrunk offsets; W-next #5 ADJUDICATED CLOSED-narrowed — the explicit wire is the supported path, the unset "clobber" is correct reconciliation, standing rule: claimed-prop wiring must be a prop; drift #11 second catch (42px docstring vs 40.0 measured); the hue-runtime caveat LOW; the fill-label-ink freeze — W-next #7 fourth voice) → 2nd QUEUED
- progress: coded Tier-2 (vellum 34 — TWO CLOCKS, only one real: the indeterminate sweep 0.9s/linear (4s under reduced-motion) is live, but the determinate fill's authored 200ms cubic-bezier is INERT in Chromium (native progress value paint doesn't transition; pixel-sampled 10%→87% within one frame; craft note: computed styles on ::-webkit-progress-value fall back — PIXELS are the truth instrument); the frozen-ink seam THIRD instance (fill re-derives while track/frame/label inks keep light literals) → W-next #7 opened; density paint-invariant (fixed 4px unit × 2.5 = 10px every rung); radius seam 4/9px live; native a11y measured (implicit progressbar, indeterminate −1, role=status "42%"); duplicate-id HARD ASSERT institutionalized) → reviews QUEUED
- scroll-virtual: coded Tier-2 (quill 30 — window-vs-content measured (spacer 399,999.97px@10k → 4,000,000@100k, served rows 14→20 constant); scrollToIndex exact (4999×40=199,960, delta 0, firstIdx=N−overscan); ENGINE-WRAPPER stamps (composed root only — data-density/dark/thumb-radius; spacer/rows carry nothing); THE RADIUS SEAM FIND: source passes typeof number — named steps die silently, a landed number is ScrollArea's W3-D2 THUMB chrome param, NOT the §3 corner stamp ("NOT the radius axis" receipt); query() two-lane live) → reviews QUEUED
- prototype-flex: coded Tier-2 (vellum 35 — THE RIG built and measured end-to-end (five stage controls 1:1 bound: flex-direction/justify/gap/row-wrap all computed-verified + clean reverts); PLAYGROUND-LAZINESS bank: ComponentCanvas mounts snippet content lazily — controls belong in the stage body; AXES = FORWARDERS (the zero-translation lane: carrier-stamps with zero readers, an inline-style-only family); QUERY BOUNDARY typecheck-proven: gap (number|string) REJECTS QueryResult, size accepts — "lanes take query(); passthroughs don't" (doctrine candidate); a11y row-reverse vs DOM-order trade (WCAG 1.3.2); 4 open questions) → reviews QUEUED
- prototype-grid: coded Tier-2 (vellum 36 — the flex harness copy-adapted; TRACK GEOMETRY resolved (cols=3 → [240,240,240]px; auto-fit floor [145px×4]@760; verbatim 1fr 2fr 1fr → [180,361,180]; gap absorbed 240→228); the three alpha laws + minmax(0,1fr) no-blowout + the areas-as-string v0 limit; EACH_KEY_DUPLICATE hydration-abort incident caught+fixed — SSR perfect, children vanish page-wide, svelte-check blind, only the live probe catches it → LAW #18 candidate) → reviews QUEUED
- reference: marginalia 1st **PASS** (the inheritance pole digit-exact both contexts; bridge-only attribution refined to NOTHING-DECLARES; the MINOR: the "unnumbered" posture specimen can't show the bare-title fallback inside a domain — moves/rewords at closure; the stale 5-vs-11 prerendered count — quill's authored number falsified by the served receipt, scoreboard; adjudications: bare-Figure honesty ACCEPTED, rest-less interface ACCEPTED as the declared exception) → 2nd QUEUED

## RULINGS: a11y floor (24 = WCAG 2.5.8 AA minimum; 28 = fleet default exceeding) · toc/See-also: chrome, OUT of the toc · component fixes: three-strikes → fix now (EXTRA lane, hardening pins, structural arm precedents) · reviewer page-edits: flag-only, route through owner/orchestrator

## Law library (16): SSR ground truth · consumed-vs-supply · canvas-id swap · declaring-element · measurement-first · composed-consumer · resolved-record · query() both-args · grep-receipt (SHAPE-AGNOSTIC) · disagreement-probe · EXTRA-lane (reference-identity, spec-pinned) · single-evaluate probes · TRANSITION-FRAME · PROBE-READINESS · FIRST-PAINT-BASE (§9.1: raw SSR serves the base; hydrated probes for flip claims; name your medium) · THEME-SPLIT (five mechanisms: raw-flip / typed-frozen / bridge-only / dropped-own-adaptation / observer-consumed; the typed-layer cut is DECLARATION COVERAGE: typed-REDECLARED-per-scope re-derives vs typed-ROOT-ONCE freezes — quill's kbd adjudication)
## Defect classes: measured-must-reproduce · rig-must-perform-its-claim · floor-asymmetry · law-true-rig-broken
## Ledgers: drift 11 · W-next 7 (semantic-ink gap · theme-split refinement · TokenTable description-field arm — structural half FIXED · dev CSSOM transient)
