# Family-comment drift ledger (orchestrator-maintained; post-campaign cleanup batch)

Found by the campaign's doc work — family SOURCE comments contradicting code/CSS.
Doc pages must document the CODE's truth and cite the census; these comments get
fixed in one cleanup batch AFTER the campaign (family files are out of doc scope).

1. blockquote `ruleSize` doc-comment says own `1`; code owns `4` (quill, task 1).
2. blockquote `blockquote.stylex.ts` header calls `--jx-space-14` MISSING; it exists (quill, task 1).
3. accordion `accordion-defaults.svelte.ts` (+ `size` prop comment) claims "a number moves summary + body"; the family CSS reads rem vars — size paints nothing there (marginalia, task 1).
4. alert family comment claims "size moves title/body"; the atoms are rem-anchored — only the root font-size moves (vellum, task 1: live-measured, source-read wrong).
5. anchor.svelte JSDoc claims size moves what density actually moves — the wrong artifact vs the rem-based channels (quill review of anchor; page fixed by scribe, family comment left).
6. checkbox.svelte `size` comment claims "CONSUMED by the family" — live-probed FALSE: the number lane is inert through the real carrier path (box/lane/label byte-identical at coefficient 1.5; channels declared at :root + rung scopes, substitution runs at the declaring element) — ledger #3/#4/#5 class (scribe, task 8: source-read wrong, measurement-first overturned it).

6. avatar.svelte:249 `onerror={handleError}` EventHandler typing error + :191 `Object.entries(style)` undefined-guard miss (both W3 batch B @4a96996f, pre-page; no runtime failure; scribe's avatar review) + :146 provideUniversalLanes non-reactive capture warning (probe says the number-lane seam works; owner to confirm deliberate).

7. avatar §11 carrier echo caps nothing: `stampCarriersForLanes` stamps `font-size: var(--jx-size-effective, 1rem)` inline on the root, beating the stylex class's `--jx-text-label-lg` — initials render at the FULL box edge at explicit sizes (48px font in a 46px box, scrollWidth 52 > clientWidth 46, visibly clipped); the page meta's "never overflows" is false on the number lane. Family-level question: cap the echo or halve by measured width (marginalia's avatar 2nd review; the heading page documents the same mechanism as INTENDED ladder replacement — here it's a defect).

8. button-group.css:210-217 dead veil gate (pre-unification names .jx-btngroup-scroll-host / [data-jx-btngroup-run] / .jx-btngroup-veil-layer match nothing — component stamps jx-scroll-host + data-jx-scroll-run; live gate in scroll-run.css:243-251/:395-399) + button-group.svelte pre-existing type errors (:325 ButtonGroupScrollEffect unresolved in instance script, :540 cx Object.entries overload, :570/:636 .style on un-narrowed Element) — scribe's button-group review; same cleanup class as #6.

9. IMAGE META EXPANSION (must land together): image has NO generated meta and sits OUTSIDE the ambient-vocabulary carriers bijection set (96 families) — generating the meta injects it into the extractor carriers set and fails the exactly-known-carriers pin; the page ships a hand PropsTable with the universal directive (marker preserved for 110/110). The meta + carriers-set expansion is a fleet follow-up PAIR (quill's image task; docs-only honored).

10. color-picker.svelte:180-181 props comment still claims the color axis is CONSUMED by the family — falsified by zero-reader grep (stamped never consumed; the page flags it, but the living family comment still teaches the overturned model to every source reader) — marginalia's color-picker review; same class as #3-#6.

11. NUMBER-INPUT STALE FAMILY COMMENTS (marginalia's review 36, source-cited): the family docstring still says "28px-wide stepper buttons" (:7) and marks size/color "CONSUMED by the family" (:103/:116) — both falsified by the page's measured rows (full-hit squares 24–48px across five rungs; zero channel readers, re-grepped). The page purged the vocabulary; the family comments didn't — and they SHIP through the same-source drawer, so the falsified claims are user-visible. FIXED at number-input closure #37 (both copies — apps/www + the registry mirror, byte-identical pre and post; scribe's mirror pre-check honored). SECOND catch: press-button's "42px" docstring (marginalia's review 38) rode that page's closure — FIXED (80720a6a: the density hit channel truth, 40.0px measured at default, both copies byte-identical). The class rule stands: the vocabulary sweep must include FAMILY COMMENTS when the drawer is same-source.
## W-next protocol findings (escalated to Owner at campaign close)
1. SEMANTIC-INK DARK GAP (both anchor reviewers, independently measured): a plain `.dark` on a component root flips the RAW token layer but never re-substitutes the `--jx-*` semantic inks (declared only at :root + stylex theme scopes) — the same :root-substitution disease W6 fixed for fill/tonal/outline in press-button.css, never fleet-wide for semantic ink. Pages must document theme as supply-side until the protocol pass.
2. THEME-SPLIT REFINEMENT (vellum's alert re-probe, 11/11 ×2): the .dark gap is NOT binary. Vars declared in jixoai.css's `:root,.jx-light,.dark` slot blocks DO re-theme at a component .dark (alert's --jx-tonal measured flip); the frozen layer is precisely the stylex defineVars emission (`:root, .xbpgcew` + theme classes — never plain .dark). PAGE LAW: one grep (which selector list declares each voice) decides the wording — name WHICH voices flip vs stay, per family.
   REFINEMENT (quill's hero-section + vellum's empty review, independently): the split also runs along the EMISSION FORM — raw `var()` string emissions re-substitute at the consuming element (flip under .dark); defineVars/typed-intermediate emissions (`--jx-x: var(--raw)` at :root) substitute ONCE at :root and a scoped .dark can never re-open them (freeze). Grep the emission, not just the selector.
3. TOKENTABLE DEAD TEXT COLUMN (marginalia's cascader review, SSR-proven): TokenTable accepts a `description` per row but never renders it, and `sourceLabel('structural')` returns '' — every fixed-paint table shows an empty Source cell. Pages must put facts in the Default column (badge's ladder does); the component gains a real description column or drops the dead field in the W-next pass. CONVERGENCE (scribe's button-group review): `source: 'structural'` is ALSO accepted by the type union but unmapped by sourceLabel (token-table.svelte:42 vs :105) — the dead-column disease has two arms (description + sourceLabel); fix both or drop both in one W-next pass. STATUS (mid-campaign): the sourceLabel arm is FIXED in-campaign (3d860a69 — the structural branch, spec-pinned 2/2, healing 27 pages); the description arm remains open and AUTHOR-SIDE-CONFIRMED (scribe's code-card 2nd: a theming-table description append rendered nothing in SSR — reverted rather than ship dead data).
4. DEV CSSOM TRANSIENT INFIDELITY (vellum's avatar task, observed twice): mid-injection dev CSSOM inlines raw chains — a `.dark` island transiently flipped a `--jx-*` border, then stabilized to the built frozen semantics. Stable dev + dist agree; TRANSIENT states can make theme probes and dev dark previews lie. Kernel/dev-runtime investigation for the W-next pass.
5. PRESSBUTTON CLAIMED-PROP CLOBBER (quill's popconfirm CODE, probe-signatured): PressButton declares popovertarget as its own prop but renders it with default undefined — its press/hover re-render on the opening click lets Svelte reconciliation REMOVE the claimed-but-undefined attribute, clobbering the family's setAttribute. Signature: aria-controls survives while popovertarget reads null; zero :popover-open panels on click. The page-side explicit wire (id + popovertarget={id} through the first-class prop) is the documented workaround on popconfirm; the auto-wire is inherently unstable behind claimed-attr components. FAMILY DESIGN DECISION for the Owner: render-when-defined or rest-spread in PressButton; the composition-d fixture ships the same dead bare pattern unpinned (asserts only the onclick path) — pin it when the family fix lands.
   STATUS (CLOSED, narrowed to documentation-closure — marginalia's press-button 1st review adjudication, receipts composed): the EXPLICIT popovertarget path (declared+forwarded, opens+toggles zero-listener) is the supported wire; the unset-path "clobber" is a claimed prop correctly winning Svelte reconciliation — a family-side fix would fight the framework. Standing rule: claimed-prop components must expose the wiring as a PROP; imperative setAttribute wires are unsupported. Both popconfirm and press-button teach it. The composition-d dead pattern stays unpinned by design (it tests the plain-button path).
6. ATOM-VS-ATOM CASCADE KILL ON STATE BORDERS (scribe's input-otp 2nd + vellum's number-input CODE, independently; root-caused + FIXED family-side by orchestrator): conditional stylex border atoms (invalidBorder's border-style: dashed) tie with the base solid atom on specificity and LOSE to the lexicographic sheet order — solid > dashed, always, regardless of DOM class order. Production emits both rules (shared atom chunk); dev additionally shows a registration gap (the atom class applied with no rule at all). Both families fixed with UNLAYERED css carve-outs (input-otp.css :where(.jx-otp-slot[data-jx-otp-invalid]) etc.; number-input.css .jx-num[data-jx-num-invalid]) — unlayered beats the components.stylex.* layers by layering; live-verified dashed renders. FAMILY/PATTERN DECISION for the Owner: state borders should follow the sibling pattern (attribute-selector css rules, e.g. input.css's [aria-invalid=true]) — or the jixoai-stylex plugin should assign conditional state atoms a higher priority layer / warn on same-property atom conflicts. Sweep candidates: any family styling state borders as conditional atoms.
7. FROZEN-INK SEAM ON COMPOSED SURFACES (opened by vellum's progress CODE; confirmed by scribe's menubar 2nd with the source line — menubar.stylex.ts:58-59 atom freeze vs menubar.css hover raw chain): one part of a composed surface re-derives dark while sibling voices keep light literals — W-next #2's :root-substitution freeze landing INSIDE a single component. Voices so far: menubar triggerOpen band · press-button outline/ghost label inks · progress track/frame/inks · number-input cell ink (the FIRST HARD LEGIBILITY FAILURE — dark value invisible 1:1 on the frozen white shell; page clause corrected + cost disclosed at closure #37; family half rides this ruling).
   EXTENSION (marginalia's press-button 1st): the fill rung's label ink is constant oklch(0 0 0) including on the re-tinted dark fill — the loudest rung's freeze; NAMED IN-PAGE at closure (80720a6a: the theme row + receipt paragraph carry it). Fifth voice. FAMILY/PATTERN DECISION for the Owner: the semantic-ink re-derivation pass (W-next #1's protocol) is the systemic fix; per-voice page disclosure is the interim.
SEVENTH CONFIRMATION + THE CENSUS (vellum's spin CODE, task 41): spin's --jx-primary: var(--primary) held oklch(0.6489 0.237 154) under a scoped .dark while inherited --primary re-derived — the root-pinned alias on a SECOND family. The class census: 116 `: var(` chains declared at :root fleet-wide — the W-next #7 sweep's scope, now numbered.
12. SPIN STALE FAMILY COMMENT (vellum's spin CODE, task 41): spin-defaults claimed "the indicator never stamped data-density and does not start now" while the composed root stamps the resolved rung today — FIXED MIRRORED at integration (the comment now distinguishes rung-opinion absence from the real data-density stamp; both copies byte-identical). Same class as #11: family comments drift while code moves.
SIXTH VOICE + SECOND MECHANISM SHAPE (vellum's separator CODE, task 40): solid separator FROZEN under a scoped .dark island — inherited --border flipped oklch(1 0 0) while the fill held oklch(0 0 0); flips only at root-level dark. Mechanism: --jx-border: var(--border) substitutes AT :root — ROOT-PINNED ALIASES can't re-derive mid-tree. A distinct shape from the defineVars freeze (the alias chain itself is the pin); grep for `: var(` chains declared at :root to find the class.
   DEEPENING (marginalia's separator 1st, task 61): the root-level-dark half is ALSO falsified on the paint — at html.dark the alias re-derives at :root (html's --jx-border reads oklch(1 0 0)) but the STRIP's computed var and painted fill HOLD oklch(0 0 0), twice measured (island-wrapped and unwrapped). SOLID'S ESCAPE HATCH DOES NOT ESCAPE — the ink never re-derives anywhere on the served tree (suspected stylex @property-style pin, stated without a cause claim). CRAFT LAW (the var-vs-paint read-site split): a receipt quoting a var flip must name WHERE the var was read — html-level reads are not the element's paint; separator corrected on three seats. The root-pinned-alias receipts on spin/tabs quoted root-dark flips — the 2nd reviews adjudicate paint-vs-var on each.
   SEVENTH VOICE + THE SECOND AA FAILURE (vellum's toast CODE, task 44): outline ink on a dark island measures **2.81:1** — frozen :root ink over the re-derived dark ground (contrast via oklch→sRGB, the helper's own oklch-as-sRGB parser bug caught and fixed mid-probe: garbage 1.64 → real 4.57 for the tonal arm). The toast card shows BOTH mechanisms at once (root-dark re-derives popover/surface WITH a mid-tween repaint; a dark island under light root re-derives ground+tonal while popover aliases hold frozen white). Fix shape: stamp the ink pair under class:dark or re-declare in the theme-scope list — family/stylesheet side.
   THE ADJUDICATION (marginalia's spin 2nd, task 65 — her law, her execution): FLEET-WIDE FROZEN for the ROOT-PINNED-ALIAS CLASS, no carve-out. The A/B in one session: the spin glyph's computed color AND the separator fill both HOLD at the element under html.dark while the html-level aliases re-derive — scribe's 'root html.dark makes it FLIP' was an html-level var read (instrument gap, not a page defect; the TWO-READ PROTOCOL — element paint + html var — is the standing instrument). Spin's five seats corrected (spin closure #50); the family defect (primary-hued loaders light-frozen on dark grounds) is W-next #7's, now measured on two families. RECONCILIATION (orchestrator, with scribe's tags-input receipt): the correction scopes to the root-pinned-alias class — chains declared in jixoai.css's `:root,.jx-light,.dark` SLOT BLOCKS legitimately re-derive at html.dark per W-next #2's declaration-coverage cut (tags-input's --jx-muted chip flip at the paint is that class, not a contradiction).
   FOURTH DATA POINT + THE FLIP MECHANISM (scribe's tabs 1st, task 68): tabs FLIPS at the paint with the chain named — --jx-foreground: var(--foreground) declared at :root, and :root IS the html element, so html.dark's re-declaration of --foreground re-derives the alias AT THE ROOT and the resolved value inherits to every element (trigger ink measured oklch(1 0 0) at the element). FLEET LINE (all paint-true): separator HOLD (--jx-border) · tags-input FLIP (--jx-muted) · tabs FLIP (--jx-foreground, the :root==html mechanism) · spin HOLD (--jx-primary — the stylex theme-class emission never matches plain html.dark, something re-pins mid-tree). The boundary is CHAIN-SPECIFIC: which selector list re-declares the UNDERLYING var, and what re-pins the alias between root and element. The Owner's W-next #7/#2 merge point.
   FIFTH DATA POINT (scribe's separator 2nd, task 70): system-dialog FLIPS on --jx-foreground — the SAME chain as tabs, the :root==html mechanism's second confirmation. FLEET LINE final for the round: separator HOLD (--jx-border) · tags-input FLIP (--jx-muted) · tabs FLIP (--jx-foreground) · system-dialog FLIP (--jx-foreground) · spin HOLD (--jx-primary). The two --jx-foreground receipts agreeing strengthens the chain-class model; census counts drift with commits (117/116/113 at read time) — served-row counts quote at-read-time.
   THE HOST LAYER (quill's terminal-footer CODE, task 67): the theme-split's seventh stratum — LOCATION BEATS THE PROP. The component-canvas stage pins data-theme="light" + .jx-light, an island that RE-PINS the light profile: page --border flips white at body/main while the served demo footer stays black-stroked, and the SAME rendered footer cloned to body level re-derives white. A component-level .dark stamp (the theme prop's class:dark, closest scope) re-derives in place. Inside the component: the stroke re-derives while --jx-muted-foreground ink freezes (the --jx-* set :root-only). Refines the sixth mechanism (canvas-pinned ground) into a precedence rule: closest themed ancestor wins over the page theme, the prop wins only within its own scope.
8. CANVAS-HOST WIDTH ATOM LOSS (quill's sheet CODE, receipted): a sheet declared inside a ComponentCanvas stage loses its width atoms — identical class lists (diffed), the live dialog computes width 1280px / maxWidth 100% (full-bleed) while the page-hosted twin computes 384px / maxWidth none; a cssRules walk found NO matching author rule (suspect canvas containment or runtime mutation). Needs the canvas family's owner. Interim: wide demos host outside the canvas (the workbench pattern — sheet's page does).
   SPLIT + SECOND REPRO (quill's system-dialog flip, task 35): the width-atom loss PERSISTS with the anchor proven correct (offsetWidth 545 > the 384px cap, maxWidth none) — two INDEPENDENT defects, cleanly split; system-dialog is the second family repro (sheet was the first), so the item is now canvas-host vs PLATFORM-ELEMENT width atoms, not a sheet quirk.
9. BOOT-SPLASH FAMILY SPEC ABSENCE (scribe's 1st review, finding 3): boot-splash rides docs-structure's route list only; the dead-workbench regression (the exact defect the page fixed) has no automated lock. A boot-splash.spec (replay → mount → auto-dismiss → chip false; manual uncapped; the RM split; the theme pair) would pin the machine.

15. TOUR'S UNREAD --jx-radius-consumed STAMP (vellum's tour CODE, task 45): the stamp lands verbatim (calc(10px * 1)) but the computed radius reads 0px — NO reader rule exists in the tour/surface chain (every sibling family carries its own reader). One missing reader rule vs a vestigial stamp — the Owner's call. Same stamped-never-consumed class as the color-picker/skeleton finds.
14. SPIN HYDRATION_HTML_CHANGED WARNING (scribe's spin 1st, task 62): the {@html} sink warns on every load — the module-level svgInstanceSeq diverges between SSR order and client mount. Visually harmless (SSR nodes retained, loader runs) but console-noisy per load. Same SALT-THE-COUNTER class as W-next #11 — family owner.
13. TAGS-INPUT SUGGESTION-ROW KEY COLLISION (vellum's tags-input CODE, task 43): the chips key by the composite `${value}#${index}` (duplicate tag values cannot collide — measured), but the SUGGESTION rows key by value alone — two identical single-word reinforcement candidates would produce duplicate each keys (LAW #18's abort class, latent: the shipped suggestion source dedupes in practice). Family-side key fix when the family is next touched.
10. TABLE OPINION-SERIES DEFECT (quill's table CODE, task 36): DENSE IS A VISUAL NO-OP at the family's own sm density — the gap channel equals the inset channel (both 8px; row height 34px either way). Design call: dense should differentiate gap vs inset (Owner-grade; the page documents the collision).
12. SCROLL-AREA VIEWPORT-ID SSR DRIFT (marginalia's 2nd, task 63): the module-level nextViewportId counter drifts ACROSS SSR REQUESTS — consecutive fetches hash differently (jx-scroll-viewport-89…121 → 122…; diff-isolated: zero non-id bytes change), so byte-identical warm-reload fails and the family comment's 'SSR unique + stable' is half-true. Within-render uniqueness holds; nothing user-visible breaks. Fix shape: $props.id() (the system-dialog root's per-instance form). Same instance-identity class as #11 and drift #14.
11. PLAYGROUND ROWID CROSS-CANVAS COLLISION (quill's table CODE, task 36; AT COST sharpened by marginalia's table 2nd, task 69): jx-play-row-1-label renders ×2 on pages hosting two canvases — a LIVE-TREE id defect (not print clone, not LAW #19 heading twins), and BOTH controls' aria-labelledby resolve to the FIRST label, so the second PlayRow ANNOUNCES THE WRONG NAME (the page-size control says 'frame width'). Not cosmetic — an assistive-technology cost. The playground owner's salt fix (per-instance counter scoping). Same instance-identity class as #12 and drift #14. Also: the SSR-only tasks-columns ×3 echo (self-resolving, recorded).
   NOTE (in-campaign, NOT W-next): the table radius static atom — the frame read the base token while radius={12} stamped the effective chain — was FIXED family-side in-campaign (orchestrator, with quill's probe receipts): --jx-radius-consumed stamps flat at explicit, the atom falls back to the site radius at auto; mirrors byte-identical; LAW #20 smoke 4×200.
