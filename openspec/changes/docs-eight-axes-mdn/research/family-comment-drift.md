# Family-comment drift ledger (orchestrator-maintained; post-campaign cleanup batch)

Found by the campaign's doc work — family SOURCE comments contradicting code/CSS.
Doc pages must document the CODE's truth and cite the census; these comments get
fixed in one cleanup batch AFTER the campaign (family files are out of doc scope).

1. blockquote `ruleSize` doc-comment says own `1`; code owns `4` (quill, task 1).
2. blockquote `blockquote.stylex.ts` header calls `--jx-space-14` MISSING; it exists (quill, task 1).
3. accordion `accordion-defaults.svelte.ts` (+ `size` prop comment) claims "a number moves summary + body"; the family CSS reads rem vars — size paints nothing there (marginalia, task 1).
4. alert family comment claims "size moves title/body"; the atoms are rem-anchored — only the root font-size moves (vellum, task 1: live-measured, source-read wrong).
5. anchor.svelte JSDoc claims size moves what density actually moves — the wrong artifact vs the rem-based channels (quill review of anchor; page fixed by scribe, family comment left).
6. checkbox.svelte `size` comment claims "CONSUMED by the family" — live-probed FALSE: the number lane is inert through the real carrier path (box/lane/label byte-identical at coefficient 1.5; channels declared at :root + rung scopes, substitution runs at the declaring element) — ledger #3/#4/#5 class (scribe, task 8: source-read wrong, measurement-first overturned it).

6. avatar.svelte:249 `onerror={handleError}` EventHandler typing error + :191 `Object.entries(style)` undefined-guard miss (both W3 batch B @4a96996f, pre-page; no runtime failure; scribe's avatar review) + :146 provideUniversalLanes non-reactive capture warning (probe says the number-lane seam works; owner to confirm deliberate).

7. avatar §11 carrier echo caps nothing: `stampCarriersForLanes` stamps `font-size: var(--jx-size-effective, 1rem)` inline on the root, beating the stylex class's `--jx-text-label-lg` — initials render at the FULL box edge at explicit sizes (48px font in a 46px box, scrollWidth 52 > clientWidth 46, visibly clipped); the page meta's "never overflows" is false on the number lane. Family-level question: cap the echo or halve by measured width (marginalia's avatar 2nd review; the heading page documents the same mechanism as INTENDED ladder replacement — here it's a defect).

8. button-group.css:210-217 dead veil gate (pre-unification names .jx-btngroup-scroll-host / [data-jx-btngroup-run] / .jx-btngroup-veil-layer match nothing — component stamps jx-scroll-host + data-jx-scroll-run; live gate in scroll-run.css:243-251/:395-399) + button-group.svelte pre-existing type errors (:325 ButtonGroupScrollEffect unresolved in instance script, :540 cx Object.entries overload, :570/:636 .style on un-narrowed Element) — scribe's button-group review; same cleanup class as #6.

9. IMAGE META EXPANSION (must land together): image has NO generated meta and sits OUTSIDE the ambient-vocabulary carriers bijection set (96 families) — generating the meta injects it into the extractor carriers set and fails the exactly-known-carriers pin; the page ships a hand PropsTable with the universal directive (marker preserved for 110/110). The meta + carriers-set expansion is a fleet follow-up PAIR (quill's image task; docs-only honored).

10. color-picker.svelte:180-181 props comment still claims the color axis is CONSUMED by the family — falsified by zero-reader grep (stamped never consumed; the page flags it, but the living family comment still teaches the overturned model to every source reader) — marginalia's color-picker review; same class as #3-#6.

11. NUMBER-INPUT STALE FAMILY COMMENTS (marginalia's review 36, source-cited): the family docstring still says "28px-wide stepper buttons" (:7) and marks size/color "CONSUMED by the family" (:103/:116) — both falsified by the page's measured rows (full-hit squares 24–48px across five rungs; zero channel readers, re-grepped). The page purged the vocabulary; the family comments didn't — and they SHIP through the same-source drawer, so the falsified claims are user-visible. FIXED at number-input closure #37 (both copies — apps/www + the registry mirror, byte-identical pre and post; scribe's mirror pre-check honored). SECOND catch: press-button's "42px" docstring (marginalia's review 38) rode that page's closure — FIXED (80720a6a: the density hit channel truth, 40.0px measured at default, both copies byte-identical). The class rule stands: the vocabulary sweep must include FAMILY COMMENTS when the drawer is same-source.
## W-next protocol findings (escalated to Owner at campaign close)
1. SEMANTIC-INK DARK GAP (both anchor reviewers, independently measured): a plain `.dark` on a component root flips the RAW token layer but never re-substitutes the `--jx-*` semantic inks (declared only at :root + stylex theme scopes) — the same :root-substitution disease W6 fixed for fill/tonal/outline in press-button.css, never fleet-wide for semantic ink. Pages must document theme as supply-side until the protocol pass.
2. THEME-SPLIT REFINEMENT (vellum's alert re-probe, 11/11 ×2): the .dark gap is NOT binary. Vars declared in jixoai.css's `:root,.jx-light,.dark` slot blocks DO re-theme at a component .dark (alert's --jx-tonal measured flip); the frozen layer is precisely the stylex defineVars emission (`:root, .xbpgcew` + theme classes — never plain .dark). PAGE LAW: one grep (which selector list declares each voice) decides the wording — name WHICH voices flip vs stay, per family.
   REFINEMENT (quill's hero-section + vellum's empty review, independently): the split also runs along the EMISSION FORM — raw `var()` string emissions re-substitute at the consuming element (flip under .dark); defineVars/typed-intermediate emissions (`--jx-x: var(--raw)` at :root) substitute ONCE at :root and a scoped .dark can never re-open them (freeze). Grep the emission, not just the selector.
3. TOKENTABLE DEAD TEXT COLUMN (marginalia's cascader review, SSR-proven): TokenTable accepts a `description` per row but never renders it, and `sourceLabel('structural')` returns '' — every fixed-paint table shows an empty Source cell. Pages must put facts in the Default column (badge's ladder does); the component gains a real description column or drops the dead field in the W-next pass. CONVERGENCE (scribe's button-group review): `source: 'structural'` is ALSO accepted by the type union but unmapped by sourceLabel (token-table.svelte:42 vs :105) — the dead-column disease has two arms (description + sourceLabel); fix both or drop both in one W-next pass. STATUS (mid-campaign): the sourceLabel arm is FIXED in-campaign (3d860a69 — the structural branch, spec-pinned 2/2, healing 27 pages); the description arm remains open and AUTHOR-SIDE-CONFIRMED (scribe's code-card 2nd: a theming-table description append rendered nothing in SSR — reverted rather than ship dead data).
4. DEV CSSOM TRANSIENT INFIDELITY (vellum's avatar task, observed twice): mid-injection dev CSSOM inlines raw chains — a `.dark` island transiently flipped a `--jx-*` border, then stabilized to the built frozen semantics. Stable dev + dist agree; TRANSIENT states can make theme probes and dev dark previews lie. Kernel/dev-runtime investigation for the W-next pass.
5. PRESSBUTTON CLAIMED-PROP CLOBBER (quill's popconfirm CODE, probe-signatured): PressButton declares popovertarget as its own prop but renders it with default undefined — its press/hover re-render on the opening click lets Svelte reconciliation REMOVE the claimed-but-undefined attribute, clobbering the family's setAttribute. Signature: aria-controls survives while popovertarget reads null; zero :popover-open panels on click. The page-side explicit wire (id + popovertarget={id} through the first-class prop) is the documented workaround on popconfirm; the auto-wire is inherently unstable behind claimed-attr components. FAMILY DESIGN DECISION for the Owner: render-when-defined or rest-spread in PressButton; the composition-d fixture ships the same dead bare pattern unpinned (asserts only the onclick path) — pin it when the family fix lands.
   STATUS (CLOSED, narrowed to documentation-closure — marginalia's press-button 1st review adjudication, receipts composed): the EXPLICIT popovertarget path (declared+forwarded, opens+toggles zero-listener) is the supported wire; the unset-path "clobber" is a claimed prop correctly winning Svelte reconciliation — a family-side fix would fight the framework. Standing rule: claimed-prop components must expose the wiring as a PROP; imperative setAttribute wires are unsupported. Both popconfirm and press-button teach it. The composition-d dead pattern stays unpinned by design (it tests the plain-button path).
6. ATOM-VS-ATOM CASCADE KILL ON STATE BORDERS (scribe's input-otp 2nd + vellum's number-input CODE, independently; root-caused + FIXED family-side by orchestrator): conditional stylex border atoms (invalidBorder's border-style: dashed) tie with the base solid atom on specificity and LOSE to the lexicographic sheet order — solid > dashed, always, regardless of DOM class order. Production emits both rules (shared atom chunk); dev additionally shows a registration gap (the atom class applied with no rule at all). Both families fixed with UNLAYERED css carve-outs (input-otp.css :where(.jx-otp-slot[data-jx-otp-invalid]) etc.; number-input.css .jx-num[data-jx-num-invalid]) — unlayered beats the components.stylex.* layers by layering; live-verified dashed renders. FAMILY/PATTERN DECISION for the Owner: state borders should follow the sibling pattern (attribute-selector css rules, e.g. input.css's [aria-invalid=true]) — or the jixoai-stylex plugin should assign conditional state atoms a higher priority layer / warn on same-property atom conflicts. Sweep candidates: any family styling state borders as conditional atoms.
7. FROZEN-INK SEAM ON COMPOSED SURFACES (opened by vellum's progress CODE; confirmed by scribe's menubar 2nd with the source line — menubar.stylex.ts:58-59 atom freeze vs menubar.css hover raw chain): one part of a composed surface re-derives dark while sibling voices keep light literals — W-next #2's :root-substitution freeze landing INSIDE a single component. Voices so far: menubar triggerOpen band · press-button outline/ghost label inks · progress track/frame/inks · number-input cell ink (the FIRST HARD LEGIBILITY FAILURE — dark value invisible 1:1 on the frozen white shell; page clause corrected + cost disclosed at closure #37; family half rides this ruling).
   EXTENSION (marginalia's press-button 1st): the fill rung's label ink is constant oklch(0 0 0) including on the re-tinted dark fill — the loudest rung's freeze; NAMED IN-PAGE at closure (80720a6a: the theme row + receipt paragraph carry it). Fifth voice. FAMILY/PATTERN DECISION for the Owner: the semantic-ink re-derivation pass (W-next #1's protocol) is the systemic fix; per-voice page disclosure is the interim.
SEVENTH CONFIRMATION + THE CENSUS (vellum's spin CODE, task 41): spin's --jx-primary: var(--primary) held oklch(0.6489 0.237 154) under a scoped .dark while inherited --primary re-derived — the root-pinned alias on a SECOND family. The class census: 116 `: var(` chains declared at :root fleet-wide — the W-next #7 sweep's scope, now numbered.
12. SPIN STALE FAMILY COMMENT (vellum's spin CODE, task 41): spin-defaults claimed "the indicator never stamped data-density and does not start now" while the composed root stamps the resolved rung today — FIXED MIRRORED at integration (the comment now distinguishes rung-opinion absence from the real data-density stamp; both copies byte-identical). Same class as #11: family comments drift while code moves.
SIXTH VOICE + SECOND MECHANISM SHAPE (vellum's separator CODE, task 40): solid separator FROZEN under a scoped .dark island — inherited --border flipped oklch(1 0 0) while the fill held oklch(0 0 0); flips only at root-level dark. Mechanism: --jx-border: var(--border) substitutes AT :root — ROOT-PINNED ALIASES can't re-derive mid-tree. A distinct shape from the defineVars freeze (the alias chain itself is the pin); grep for `: var(` chains declared at :root to find the class.
   DEEPENING (marginalia's separator 1st, task 61): the root-level-dark half is ALSO falsified on the paint — at html.dark the alias re-derives at :root (html's --jx-border reads oklch(1 0 0)) but the STRIP's computed var and painted fill HOLD oklch(0 0 0), twice measured (island-wrapped and unwrapped). SOLID'S ESCAPE HATCH DOES NOT ESCAPE — the ink never re-derives anywhere on the served tree (suspected stylex @property-style pin, stated without a cause claim). CRAFT LAW (the var-vs-paint read-site split): a receipt quoting a var flip must name WHERE the var was read — html-level reads are not the element's paint; separator corrected on three seats. The root-pinned-alias receipts on spin/tabs quoted root-dark flips — the 2nd reviews adjudicate paint-vs-var on each.
   SEVENTH VOICE + THE SECOND AA FAILURE (vellum's toast CODE, task 44): outline ink on a dark island measures **2.81:1** — frozen :root ink over the re-derived dark ground (contrast via oklch→sRGB, the helper's own oklch-as-sRGB parser bug caught and fixed mid-probe: garbage 1.64 → real 4.57 for the tonal arm). The toast card shows BOTH mechanisms at once (root-dark re-derives popover/surface WITH a mid-tween repaint; a dark island under light root re-derives ground+tonal while popover aliases hold frozen white). Fix shape: stamp the ink pair under class:dark or re-declare in the theme-scope list — family/stylesheet side.
   DIGIT RECONCILIATION (scribe's toast 1st, task 71, corrected OKLab→LMS³→linear-sRGB path): at the CURRENT token vintage the failing pair measures **1.17:1** — frozen `--jx-popover-foreground` oklch(0% 0 0) over the re-derived ground (Y 0.00862); `--jx-muted-foreground` on the same ground 1.42:1; the re-derived TONAL ink 6.21:1 (fine). The billed 2.81:1 did not reproduce (pair identity or token-vintage skew); both readings are RED — the seventh voice stands with the WORSE digit. The page quotes no absolute digit (verified — ledger-only reconciliation, no page edit owed).
   EIGHTH VOICE, THE FIELD FAMILY'S L3 (marginalia's textarea 2nd, task 84): the settled theme="dark" stamp flips the shell face (base tokens) while the TYPED TEXT rides the root-pinned alias chain and holds the root resolution — black text on the flipped shell, the seam landing on user content, not chrome. LATENT on the page (no served L3 demo) but now TAUGHT: the theme row carries the two-halves clause at closure #58. Same fix shape as the toast voice (the ink pair re-declared where the stratum stamp lands).
   THE ADJUDICATION (marginalia's spin 2nd, task 65 — her law, her execution): FLEET-WIDE FROZEN for the ROOT-PINNED-ALIAS CLASS, no carve-out. The A/B in one session: the spin glyph's computed color AND the separator fill both HOLD at the element under html.dark while the html-level aliases re-derive — scribe's 'root html.dark makes it FLIP' was an html-level var read (instrument gap, not a page defect; the TWO-READ PROTOCOL — element paint + html var — is the standing instrument). Spin's five seats corrected (spin closure #50); the family defect (primary-hued loaders light-frozen on dark grounds) is W-next #7's, now measured on two families. RECONCILIATION (orchestrator, with scribe's tags-input receipt): the correction scopes to the root-pinned-alias class — chains declared in jixoai.css's `:root,.jx-light,.dark` SLOT BLOCKS legitimately re-derive at html.dark per W-next #2's declaration-coverage cut (tags-input's --jx-muted chip flip at the paint is that class, not a contradiction).
   **THE ALIAS-THEME LAW (three-tier; the task-74 re-adjudication — separator/spin/tags-input, census + ancestor-walk + body-level-clone instrument).** The --jx-* aliases (`:root, .xbpgcew { --jx-border: var(--border); --jx-primary: var(--primary); --jx-muted: var(--muted); … }`, @layer components.stylex.priority1) are LIVE at the root: a var() in a :root custom property re-substitutes when html.dark flips the underlying site token, and the recomputed value inherits to every unpinned descendant.
   — Tier 1, ROOT DARK FLIPS: unpinned instances re-derive (separator solid oklch(0 0 0)→(1 0 0) in place and by body-level move; spin ink 33/40; tags-input chips 14/22).
   — Tier 2, ISLAND-PROOF: a mid-tree .dark island re-derives the site token but cannot re-substitute an alias declared above it (all three families + progress).
   — Tier 3, STAGE-PINNED: demo stages carrying data-theme="light" re-voice the token family mid-tree and locally pin their content (pin site: the stage div inside jx-canvas-scroll; every measured hold — 8/22 chips, 1/2 solids, 7/40 inks — had the pin ancestor; none survived a body-level move).
   The var-vs-paint read-site law stands with CORRECTED SCOPE: html var and element paint AGREE at root dark (both flip); they diverge only under islands/stages — where the single-sample probes of tasks 61/65 went astray. THE FLEET LINE RETIRES (the fourth/fifth data points' chain-class model superseded); 'root-pinned alias, frozen' in any earlier report or page copy refers to a stage pin unless verified outside a data-theme ancestor. spin's internally-inconsistent :248 seat was RIGHT against five consistent ones — adjudicate before harmonizing. THE LAW'S UNIT (marginalia's terminal-footer 2nd, task 81): the alias-theme law classifies per CHAIN, not per element — inside one footer the ghost stroke reads var(--border) directly (flips with any scope) while the meta ink reads the --jx-muted-foreground :root alias (frozen). Per-element readings were the trap; per-chain is the unit.
   Remaining sweep: tabs CONFIRMED at 2nd review (task 76 — every unpinned instance flips, the 29 holds exactly the stage pins, the fourth family clean); system-dialog (confirm at next touch) and progress (the W-next #7 frozen-ink instance) outstanding.
   THE HOST LAYER (quill's terminal-footer CODE, task 67): the theme-split's seventh stratum — LOCATION BEATS THE PROP. The component-canvas stage pins data-theme="light" + .jx-light, an island that RE-PINS the light profile: page --border flips white at body/main while the served demo footer stays black-stroked, and the SAME rendered footer cloned to body level re-derives white. A component-level .dark stamp (the theme prop's class:dark, closest scope) re-derives in place. Inside the component: the stroke re-derives while --jx-muted-foreground ink freezes (the --jx-* set :root-only). Refines the sixth mechanism (canvas-pinned ground) into a precedence rule: closest themed ancestor wins over the page theme, the prop wins only within its own scope.
8. CANVAS-HOST WIDTH ATOM LOSS (quill's sheet CODE, receipted): a sheet declared inside a ComponentCanvas stage loses its width atoms — identical class lists (diffed), the live dialog computes width 1280px / maxWidth 100% (full-bleed) while the page-hosted twin computes 384px / maxWidth none; a cssRules walk found NO matching author rule (suspect canvas containment or runtime mutation). Needs the canvas family's owner. Interim: wide demos host outside the canvas (the workbench pattern — sheet's page does).
   SPLIT + SECOND REPRO (quill's system-dialog flip, task 35): the width-atom loss PERSISTS with the anchor proven correct (offsetWidth 545 > the 384px cap, maxWidth none) — two INDEPENDENT defects, cleanly split; system-dialog is the second family repro (sheet was the first), so the item is now canvas-host vs PLATFORM-ELEMENT width atoms, not a sheet quirk.
9. BOOT-SPLASH FAMILY SPEC ABSENCE (scribe's 1st review, finding 3): boot-splash rides docs-structure's route list only; the dead-workbench regression (the exact defect the page fixed) has no automated lock. A boot-splash.spec (replay → mount → auto-dismiss → chip false; manual uncapped; the RM split; the theme pair) would pin the machine.

16. TABS-LIST MISSING IMPORT (marginalia's tabs 2nd, task 76): `Cannot find name 'TabsScrollEffect'` at tabs-list.svelte:158 — a one-line missing import, type-layer only (the scroll demo runs). The family-owner class; same cleanup lane as the Object.entries overloads.
15. TOUR'S UNREAD --jx-radius-consumed STAMP (vellum's tour CODE, task 45): the stamp lands verbatim (calc(10px * 1)) but the computed radius reads 0px — NO reader rule exists in the tour/surface chain (every sibling family carries its own reader). One missing reader rule vs a vestigial stamp — the Owner's call. Same stamped-never-consumed class as the color-picker/skeleton finds.
14. SPIN HYDRATION_HTML_CHANGED WARNING (scribe's spin 1st, task 62): the {@html} sink warns on every load — the module-level svgInstanceSeq diverges between SSR order and client mount. Visually harmless (SSR nodes retained, loader runs) but console-noisy per load. Same SALT-THE-COUNTER class as W-next #11 — family owner.
13. TAGS-INPUT SUGGESTION-ROW KEY COLLISION (vellum's tags-input CODE, task 43): the chips key by the composite `${value}#${index}` (duplicate tag values cannot collide — measured), but the SUGGESTION rows key by value alone — two identical single-word reinforcement candidates would produce duplicate each keys (LAW #18's abort class, latent: the shipped suggestion source dedupes in practice). Family-side key fix when the family is next touched.
10. TABLE OPINION-SERIES DEFECT (quill's table CODE, task 36): DENSE IS A VISUAL NO-OP at the family's own sm density — the gap channel equals the inset channel (both 8px; row height 34px either way). Design call: dense should differentiate gap vs inset (Owner-grade; the page documents the collision).
12. SCROLL-AREA VIEWPORT-ID SSR DRIFT (marginalia's 2nd, task 63): the module-level nextViewportId counter drifts ACROSS SSR REQUESTS — consecutive fetches hash differently (jx-scroll-viewport-89…121 → 122…; diff-isolated: zero non-id bytes change), so byte-identical warm-reload fails and the family comment's 'SSR unique + stable' is half-true. Within-render uniqueness holds; nothing user-visible breaks. Fix shape: $props.id() (the system-dialog root's per-instance form). Same instance-identity class as #11 and drift #14.
11. PLAYGROUND ROWID CROSS-CANVAS COLLISION (quill's table CODE, task 36; AT COST sharpened by marginalia's table 2nd, task 69): jx-play-row-1-label renders ×2 on pages hosting two canvases — a LIVE-TREE id defect (not print clone, not LAW #19 heading twins), and BOTH controls' aria-labelledby resolve to the FIRST label, so the second PlayRow ANNOUNCES THE WRONG NAME (the page-size control says 'frame width'). Not cosmetic — an assistive-technology cost. The playground owner's salt fix (per-instance counter scoping). Same instance-identity class as #12 and drift #14. Also: the SSR-only tasks-columns ×3 echo (self-resolving, recorded).
   NOTE (in-campaign, NOT W-next): the table radius static atom — the frame read the base token while radius={12} stamped the effective chain — was FIXED family-side in-campaign (orchestrator, with quill's probe receipts): --jx-radius-consumed stamps flat at explicit, the atom falls back to the site radius at auto; mirrors byte-identical; LAW #20 smoke 4×200.
17. MODAL FOCUS-TRAP LEAK — THE SKIP-LINK GAP (marginalia's dialog 1st, task 85; adjudicator-confirmed live on 5245): with exactly one dialog[open] via showModal() (top layer engaged, ::backdrop painted, open-count 1 at every press), REAL Tab presses escape to the page's skip link EVERY SECOND PRESS (per-press receipts, two independent runs + the orchestrator's reproduction: in-dialog tabbable → skip link, contained:false → in-dialog tabbable → …). The skip link is the ONE page focusable the modal inertness does not cover; from it the next press returns to the dialog — the rest of the page IS inert. The plain `<a href="#main">` markup (website-scaffold.svelte:304) carries nothing special; no cause claim (the separator SOLID precedent). Page claims corrected (the a11y row, the canvas description, the family comment — mirrored); the repair (inert polyfill or scaffold-side skip-link handling while a modal is open) is the family/scaffold owner's. system-dialog unaffected (non-modal by design — popover base, Tab free, truthful).
18. DIALOG ELEVATION STAMPED, NOT PAINTED (marginalia's dialog 1st, task 85): the level carriers stamp the top-layered root but the shadow/rung recipe never paints — no box-shadow, filter, backdrop-filter, or background-image on any channel (dialog, [data-jx-card] child, .jx-surface-shadow child, ::before/::after) at settle across all five universal seats; level4/level3/6dp/level-1/dark visually identical, the only fill the translucent oklch(1 0 0 / 0.32) layer. The one elevation-adjacent lane that DOES paint: radius (20px seat, the auto child 6px = max(0, 20−14) digit-exact — the concentric law through the top layer). Same owner-decision shape as #15: wire the shadow reader or retire the rung receipts. OPEN ADJACENT (not adjudicated): the same walk found no settled ::after shadow on the solid seats — the jx-surface law's own 'REAL ::after hard offset shadow' claim is unverified at settle; the family owner's probe decides whether that is the same unwired reader or a separate channel.
   THIRD FAMILY (marginalia's float-button 1st, task 86): the FAB stamps --jx-elevation-effective = 6 (the own level3, measured) but --jx-shadow-effective ships EMPTY — the stamp arrives, the recipe drops; the menu's [data-jx-fab-menu-shadow] child (the comment's 'real shadow layer') animates in lockstep but paints a translucent wash (box-shadow none); zero shadow channels on button or panel. Page claims trued + family comment carries the measured caveat (mirrored).
19. SCROLL-DRIVEN REVEAL = PERMANENT CONTAINING BLOCK (marginalia's float-button 1st exposed the geometry break; orchestrator root-caused live):
   THE FLEET SWEEP (marginalia's T126, 2026-09-24): **CLEAN — ZERO HITS**. Exactly one inline fixed renderer exists in the fleet (float-button; the three no-reveal seats hold — 28 fixed elements, all revealAncestor false). Everything else structurally immune by two classes: (a) popover platform panels (tour/system-dialog/tags-input) — top-layer while open ESCAPES the CB entirely (the sysdlg anchored panel sits inside a revealed section yet measured moved 0; `:popover-open` membership is the immunity receipt for that class — the rect-across-scroll test is only decisive for PLAIN fixed elements, anchor-positioned panels move by design); (b) the float-plane adoption (toast: stack chain jx-float-slot → jx-top-layer → jx-shell, position static; the fixed standalone corners dormant). The law DEMONSTRATED on demand (in-probe reveal injection on #menu-idiom: section transform matrix(1,0,0,1,0,10.1642), the fab moves −282.6 with the content — the hijack reproduced, then reverted clean). **THE OWNER'S SYSTEMIC QUESTION ANSWERED: YES** — an opacity-only reveal (@keyframes opacity-only, same view() timeline + fill both) leaves the section's computed transform NONE and the fab FREE; the option is real, the trade is losing the translateY rise. animation:none (the RM path) = the same full escape. NITs ledger-only: marker comments at the sysdlg/tags popover-in-revealed-section demos would arm future editors; per-hit fix list NONE.
 jixoai.css's [data-reveal] entrance (animation: jx-reveal-rise both + animation-timeline: view()) leaves EVERY revealed section a containing block for position:fixed descendants FOREVER — at progress 1 the computed transform stays an identity matrix (never none), and the animation never ends. Measured on float-button.html: ALL THREE fixed fabs were section-contained (the plain idioms too — the reviewer's 'plain fabs held' was the section-bottom ≈ viewport-bottom coincidence at her scroll position; constant-across-scroll rects falsified it). fill-mode: backwards does NOT help (A/B live: the section computed transform:none yet the box stayed contained — the CB persists for the animation's lifetime); only removing the animation frees it (animation:none → the stack snapped to exactly (20, 840)). In-campaign fix: the three sections hosting live fixed fabs carry no data-reveal (the page's own walkthrough-3 comment finally applied to its own sections). OWNER DECISION: the systemic options (opacity-only variants for reveal-hosting sections, or an animation that truly ends) + the FLEET SWEEP (every page hosting a fixed-idiom component under [data-reveal]).
20. ANCHOR-FIXED FIT-CONTENT FIRST-LAYOUT STALENESS (orchestrator, task 86 consolidation, live A/B): a fixed, anchor-name-carrying flex column with width: fit-content lays out at the FULL containing-block width (1440px) at first layout — the width rule is present and matched (CDP), no recompute happens; ANY style write collapses the box to fit-content and it stays (inline write → 40px; remove the write → stays 40; fresh load → 1440 again). Reproduced identically in dev and in the built dist (a static <link> stylesheet). Workaround landed: the width is baked into the stack's SSR inline style (float-button.svelte) so first layout computes with it, plus the css-lane rule (float-button.css) documenting the law; a stylex atom carried the rule into the payload yet the box still measured stretched — same staleness, not an injection failure. Family owner: watch for the class wherever anchor-named fixed boxes size by intrinsic width.
21. THE VIEWPORT-CORNER LANE CONTEST (measured + hit-tested, task 86): on scaffold-based docs pages the sections-nav column owns x0–256 full-height and the page-toc rail owns x1231–1421 × y94–358 (CONTENT-DEPENDENT digits — vellum's T115 re-read measured the rail frame x1200 w240 digit-exact but the inner nav x1220 w212; the lane CONTEST is the law, the digits are per-content; the float-button prose hedged at her 2nd's consolidation) — a left-corner FAB paints UNDER the nav (elementFromPoint at the fab's center returns the nav's list) and a top-right FAB at the family's 5.5rem sits under the rail head. The float-button page now demos the bottom-right column stack (three fabs, the class prop's geometry channel — the documented API) and teaches both contested lanes in prose + the corner prop's row; consumers float right-side below the rail. The LANE RULING (does the float layer own the corners over the chrome? z-order, or reserved lanes?) is the Owner's.
22. THE NATIVE-PARITY GATE'S FIVE FAILURES — HARNESS vs DESIGN-ERA COLLISION (marginalia's native-select 1st, task 90; reproduced by the orchestrator on 5245, 5 failures / 251 comparisons + 20 DOM-AST): (a) input focused border hue — tier0 raw paints oklch(0 0 0) while tier1 paints the BRAND HUE (oklch(0.6489 0.237 298) at the current 24h-cycle hue): the family's :focus brand tint vs the gate's tier0==tier1 paint contract, born when the brand-hue runtime landed; (b) native-select DOM isomorphism ×4 — the stylex migration stamps atoms (x19aimcq xuwbqqx) on the NATIVE select root itself while the gate asserts the native control stays class-free (tier0 class absent). Both are OWNER DECISIONS: relax the assertions (focus-hue tolerance; allowed classes on native roots) or revert the family styling (monochrome focus; wrapper-only atoms). No prior baseline — the failures predate the campaign (tailwindless Wave 1 / brand-hue eras), surfaced by the first audit. DESIGN RECEIPT (marginalia's section-card 1st, task 97 — the DensityDemo heading-clone class): the ×4 clone shape that minted duplicate ids (the popover density-pop quartet, killed by childrenScoped) GENERALIZES to headings — a heading-bearing density seat mints its H2 ×4 in the document outline (ids stay clean, auto-generated; the authored rails unaffected; the search corpus's first-match/suffix dupe handling absorbs it). childrenScoped cannot scope this away (the headings are the demo's subject). Options for the Owner: an outline opt-out for demo-clone headings, or an accept-as-is ruling. Recorded from section-card's seat ("Acquire a Backend." ×5 in a 17-h2 census).
   PROCESS: the gate defaults to :5199 and needs a running site — a leaked design-server was accidentally serving it STALE until killed this session (task 89's orphan); standing practice: pass the port explicitly (node scripts/verify-native-parity.mjs <port>).

## Process note — the docs-structure lint reads BUILT pages (2026-09-23, float-button consolidation)
verify:docs lints `apps/www/dist` (or the prerendered dir) — gate verdicts run against whatever build exists. A stale build MASKS current-source failures: press-button's retitled Usage H2 and toast's Examples-before-Usage order were both red in source yet green under the campaign's older build, exposed only when this task's fit-content investigation forced fresh builds. HONEST GATING = fresh `npm run build` before verify:docs at every consolidation. press-button's H2 was fixed in-campaign (the descriptive title moved into the summary); toast's order fix rides scribe's 1st-review consolidation (the page is under active review — no baseline contamination).

## Process note — the cx-joiner clone class, true scope (2026-09-23, post-list consolidation)
The per-review "Nth clone" counts (hover-card #10, list #12…) were page-scoped diagnostics, not the class census. Full-tree svelte-check after closing five more seats (breadcrumb-list :39, command-list :60, tabs-list :183, blueprints scenes list :17 + list-item :40 — f92d6555, mirrored ×3): the overload class spans 416 diagnostics / 414 distinct files — overwhelmingly test/*.spec.ts page-objects plus route pages, plus ~595 textual copies of the idiom overall (most copies type `style` narrowly and never error). The `?? {}` micro-fix stays the per-family gate-zero shape (mechanical, runtime-identical); the FLEET fix is the recorded shared-util consolidation (one joiner with a type predicate on the filter, replacing every copy) — a lull/finale task, deliberately NOT run mid-flight: 414-file churn while reviewers hold probe surfaces would contaminate every open review's baseline. Non-cx typing debt found adjacent and left for its families' cycles: tabs-list TabsScrollEffect (:158) + HTMLDivElement (:410); blueprints scenes list.svelte Props typo (:37).

## Process note — svelte-check 只做必要检查（Owner 指令，2026-09-24）
全量 `npx svelte-check`（~2500 文件）资源占用过高，禁止反复跑。纪律：（1）每个整合轮次至多跑 ONE 次全量，输出落盘 /tmp 供该轮所有 scoped grep 复用 —— 绝不在同一轮内为验证单个修复重跑；（2）机械修复（`?? {}`、ternary、重命名类）当场不跑 svelte-check，验证搭该轮唯一次运行或下一批次门禁；（3）"page-scoped check" 的含义 = 对已保存输出的 grep 过滤，不是重复全量；（4）子代理简报同此：门禁回执复用单次运行，不为逐页检查重复执行。

## Platform-delta note — the top-layer hit-testing edge (quill's popover 2nd, task 114)
"Renders above open dialogs" hit-tests TRUE on system Chrome but FALSE on Chrome for Testing 153.0.8010.12 (a popover opened after showModal() enters the top layer — :popover-open true — yet elementFromPoint still hits the DIALOG, fresh seat and reopen both). Same attribution class as the engine-vintage contrast digits: the platform's own seam, not the family's. The popover claim now carries the engine-dependence wording; any future top-layer z-order claim gets engine-named receipts.

## Owner tension — the successor global-gate flip vs the UNMAPPED FLEET (scribe's website-scaffold 2nd, task 118; corrects the task-112 dispatch premise)
website-scaffold is NOT in the 11-page inScope JSON (unmapped, like dialog) AND carries the same examples-before-usage structure (measured: staging marker @26602 < Usage H2 @111676). The successor change `2026-08-30-docs-demo-standard-global-gate` flips the skeleton gate to hard-fail-everywhere once the backlog empties — that flip therefore faces the WHOLE unmapped fleet's reorder-or-scope ruling, not just dialog's (the task-112 adjudication generalized). Owner's call at campaign close: reorder the unmapped pages' Usage above their demo canvases (a fleet restructuring), or scope the global gate's inScope list.

## Skip-link probe receipt (marginalia's T128, 2026-09-24) — W-next #17's Owner decision now carries receipts
RECOMMEND landing `tabindex="-1"` + programmatic focus on main#main (with the `.jx-page-main:focus { outline: none }` companion — the candidate paints the ring around the ENTIRE main otherwise). The A/B on grid.html real keyboard drives: baseline leaves activeElement BODY (click path strands ON the link, nothing paints); the candidate lands focus on MAIN both paths, landing still exactly 74 (focusDelta 0, no scroll-padding fight), main never a tab stop in a 25-Tab walk, layout byte-identical. The sharpening: Tab-after-skip already starts inside main in BOTH arms (fragment navigation moves the sequential starting point) — the real buys are the SR landmark announcement, the un-stranded click path, visual confirmation, engine portability. If landed, the website-scaffold a11y Enter row trues UP to 'focus lands on #main.' Owner's call; shape in her report.

## Open behavior + CODE debt from T127 (2026-09-24)
- COMMAND MINOR-1 (Enter-select never closes the palette under the served default): the static chain is SOUND end-to-end (command-item.fire → ctx.close → setOpen(false) → the open-$effect's else-branch → shut() — jx-rest present in vellum's reads means shut never ran, yet every link is typed and wired). A LIVE-ONLY failure — assigned to vellum for repro+fix (reading (a) bind-back break vs (b) undocumented deferral; the api row + :50 law carry whichever truth lands).
- MARKDOWN family 47 (markdown-node ×36 ParsedNode union never narrows + parse.ts ×11 paragraph.children unknown): the fix is ONE type-guard pass over the family (per-seat casts at 36 seats would be the wrong shape) — assigned to vellum as a typing CODE micro-task.

## NEXT-ROUND QUEUE (2026-09-24, context-boundary handoff)
- TEXT MAJOR (marginalia 131): the six modifier utilities don't exist in the served sheet — the playground emits correct class strings (font-medium, [font-size:14px]) that compute nothing; seat reads 400/16px; raw-div injection proves it sheet-level; dist greps absent in BOTH builds. THE FIX IS THE SCANNER FEED (the page's own cited app.css jx-html precedent), NOT the kernel (whose string law measured exactly right). inline-code shares the kernel. Plus text NIT (the "UA baseline shift" is preflight-reimplemented).
- SCROLL-RUN (131): MINOR — chips ship hardcoded tabindex="-1" (:282/:294) vs the page teaching "real focusable buttons" in two seats; MINOR — six page type seats (:286/:301 cx-false debt class, :102 rune shadow, :109 attach, :314/:315 snippet pair); NIT — "pre-hydration naked" is paint-true/DOM-false.
- TERMINAL-HEADER (131): LOW — two seats say "click the Components pill above"; the live pill is "Docs" (one rename ×2).
- PAGINATION residual: pagination-link.svelte :93 the button spread still errors after the anchor cast + widening cast attempts (the button branch spreading anchor-typed props); one seat left of the 19 — next round's shape: omit anchor-only attrs from the button spread or type the derived as a union. NEEDS-WORK 1st (duplicate id usage killed at this consolidation; the family's 7 cx + trio closed).
- T129/T130 NITs unlanded: pattern-faq (the coincident demo pair — size 16 == medium 16px); pattern-hero-set (the composite API table's prefix convention); progressive-blur (#pblur-demo exists+data-region'd — rail entry optional); mermaid (the hand-authored canvas mirrors' resolveRawCode follow-up self-recorded).
- RESULT warning seat (130 NIT): the warning state has no served seat (three render, fourth taught-only).
- THEME-TOGGLE N (quill 133): the theme-axis demo remains the owner's upgrade call.
- icon-button tooltip re-derive was INCONCLUSIVE on quill's runner (her end-to-end receipt stands) — next prober owns.

## The site-layer hotkey collision law (vellum's T132, 2026-09-24)
command's "Enter-select never closes" was NEVER a family bug — instrumented ground truth: the full chain fires live (fire → ctx.close → setOpen → effect → shut → dialog.close → close event; the palette closes). The `dialog[open]` her T127 probes kept finding was the SITE'S OWN search palette — search-palette.svelte :227 binds ⌘K on window (mounted at the root layout), the demo opts into `hotkey`, BOTH handlers fire on one keypress, and the site search carries the kernel's jx-rest pose → the class-instrument misread it. LAW: a demo binding a fleet-shared hotkey on the same page as the site chrome DOUBLE-OPENS; class-instrument reads must identify WHICH dialog (by id/label, not pose class). The api row now teaches the collision; whether the demo drops `hotkey` is the Owner's teaching call. Also banked: Array.isArray cannot narrow `readonly ParsedNode[]` (the isNodeSequence guard); a union member not in ParsedNode (AccordionGroupNode) poisons every isNodeType call — move its branch to HEAD position.

## THE TAILWINDLESS FEED LAW (quill's T135, 2026-09-24 — text's T131 MAJOR resolved)
The site is TAILWLESS by design (app.css W4: the Tailwind engine RETIRED) — no scanner/generator exists; a kernel emitting tailwind-dialect class strings ships STRINGS THAT NOTHING COMPILES unless a real CSS rule is authored. The empirical split that pinned it: the cited app.css jx-html block "works" only because .jx-html-input etc. are REAL RULES in jixoai.css behind the names; the text page's bracket candidates lived in a .svelte COMMENT — and comment blocks feed nothing (app.css is read raw; svelte content comments are not). Fix shape: a @layer components "text-modifier kernel feed" block in app.css covering the kernel's FULL emittable vocabulary (weights/italic/trackings/leadings/families/sizes) — same layer as jx-html so consumer classes still win; the kernel untouched; inline-code shares the feed. LAW: on this tailwindless site, any class-string API needs its rules authored (or a generator) — a comment citing a precedent is not a feed.
