# Tasks

- [x] 1. 删除 ButtonBar 全影响面：12 整文件（canonical+mirror 成对）、
       registry.json 条目、svelte.config 两树、docs-structure 快照
       （general:15→14）、ambient-vocabulary carriers、
       context-coverage config/exemptions、living spec Requirement、
       blueprint SVG；code-card 未提交的 ButtonBar 用法回退
- [x] 2. Card 内核：新建 card-body.svelte（cell 滚动 + 沟槽公式单源）；
       CardHeader standalone fallback；CardFooter label 中性默认 +
       吸收确认；Card 根自举改用结构件；index 导出更新
- [x] 3. Dialog 瘦身：scroll 宿主贴 data-jx-card、head 用
       jx-card-head-grid、× 坐 jx-card-end-action-slot、CardBody、
       CardFooter 方言；删 dialog-header/footer(.css) 与 dialog.css
       B 列；A 列 flex 传导块 grid 化改造；15rem 反转统一
- [x] 4. 消费者迁移：search-palette（CardHeader）、docs dialog 页
       （Card 系 + 穿刺锚点）、blueprint scene dialog、dialog/card
       测试锚点
- [x] 5. 野生区收编：code-card foot 区域 zone + PressButton 直用
       （density sm + copied hue）；canvas dock reset → Scope +
       IconButton
- [x] 6. spec：living spec 删 button-bar Requirement（任务 1 内）+
       新增 structural kernel law（四层地图 + 贴纸法则 + 分区 zone
       法则 + attach 判据）
- [x] 7. docs：card 页（贴纸 API + standalone 结构件）、dialog 页
       （方言化）、code-card/canvas 页核对
- [ ] 8. 门禁 + 验收：vitest 全量、svelte-check、build、blueprints
       再渲染、mirror manifest/meta、verify:all（CHROME_PATH）；
       vision 子代理视觉验收（dialog/card/code-card before/after +
       窄屏反转 + 暗色 + acrylic）；general-purpose 复核
- [ ] 9. 收尾：复核结论处理、change 归档注记、双 commit（spec 先行）
       推送 main
