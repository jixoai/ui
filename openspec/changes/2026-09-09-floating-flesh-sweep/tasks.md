# Tasks

- [x] 1. PressButton/IconButton rest 透传（stamps 后展开）+ 测试；
       canvas reset wrapper hack 回退
- [x] 2. CardBody 加 class prop（cell 追加）
- [x] 3. sheet 方言化：贴纸宿主 + card.css import + head 带 zone +
       CardHeader children + × IconButton + CardBody + foot 带 zone +
       Separator；sheet.css 清皮肉；测试/docs 锚点迁移
- [x] 4. system-dialog 方言化：Action/Cancel 换 PressButton（stamp
       rest 直落）+ Content 贴纸三带 + Separator 行带；测试/docs
- [x] 5. 镜像/meta/manifest + 全量门禁（vitest/svelte-check/build/
       blueprints/verify:all——print 项为已知上游遗留）
- [x] 6. vision 验收（sheet 四向侧滑 + system-dialog + 回归页）+
       general-purpose 复核；结论处理
- [x] 7. 双 commit（spec 先行）推送 main
