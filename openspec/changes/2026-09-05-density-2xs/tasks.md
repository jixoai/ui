# Tasks

- [x] 1. Kernel tokens: the `:root` `--jx-density-*-2xs` set (grammar
      rows) + the `[data-density='2xs']` scope block with the scoped
      `--jx-hit-floor` + hit-min redeclaration — registry theme sheet +
      byte-identical www mirror
- [x] 2. Density context: `Density` union + '2xs' (both trees)
- [x] 3. Verifier: 2xs row + scoped-floor assertions in real Chromium;
      repair the stale 44/44/44 hit expectations to the 2026-08-29
      ruling (28/32/40/48) — ALL GREEN
- [x] 4. Tests: density-context 2xs resolution + scope stamping;
      density-adoption-form-text 2xs stamp case — GREEN
- [x] 5. Enumerations: density-demo type union, tokens.html kernel
      table + live demo + copy, blueprint scene, select.docs union
      text + both drift pins, press-button (both trees),
      textarea/number-input/native-select/input-otp/form/chip docs
      rows (www side); input.html exempt (parallel session)
- [x] 6. Acceptance page: /docs/density-2xs.html — live-var five-rung
      table, 2xs inspector scene beside default, adoption-law note,
      nav line in docs-route-model
- [x] 7. Verification battery: verify-density-kernel, targeted vitest,
      curl 200 on the live dev server; mirror `diff` proof
