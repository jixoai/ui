# spike/d3 — the D3 consumer wiring-cost fixtures

Change 2026-09-13-stylex-kernel-research, task D3. SCOPE: D3 measures
WIRING cost on a constant payload (the 8 frozen families); the registry
payload mechanism itself is engine-invariant (proven by verify:shadcn-add).

| dir | configuration |
|---|---|
| `tw-baseline/` | TW4 today: @tailwindcss/vite plugin line + app.css import; registry TW forms (closure copy) |
| `arch-a/` | consumer HAND-WIRES @stylexjs/unplugin (config block + dev-HMR snippet hand-copied) |
| `arch-b/` | `jixoai-ui-vite-plugin` stub (packages-stub/, npm-pack tgz) absorbs ALL wiring — consumer adds 1 package + 1 vite.config line |
| `arch-c/` | precompiled: class-constant modules + stylex.css (generated by `precompile/`); consumer keeps only the @stylexjs/stylex RUNTIME + 1 css import |
| `packages-stub/` | the arch-B plugin stub (research only, never published) |
| `precompile/` | the arch-C kernel-build-side tool (NOT in any consumer closure) |

Measurement scripts (frozen protocols): `measure-bytes.mjs` (F5 bytes),
`extract-lockfile-names.mjs` (F6 packages), `run-schedule.mjs` (the
40-build rotation). Raw receipts: `logs/`. Verdict report:
`../../research/d3-consumer.md`.

Payload trims disclosed in the report (shiki backend seam → null
backend; @fontsource imports neutralized) — payload parity across all
four configurations.
