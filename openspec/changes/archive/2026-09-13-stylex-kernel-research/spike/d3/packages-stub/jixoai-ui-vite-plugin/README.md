# jixoai-ui-vite-plugin (D3 research stub)

Architecture B's distribution plugin, stubbed for the D3 measurement (change
2026-09-13-stylex-kernel-research). NOT published; consumed by d3/arch-b via
`file:../packages-stub/jixoai-ui-vite-plugin`. Its dependency closure (@stylexjs/unplugin
0.19.0 + @stylexjs/stylex 0.19.0) COUNTS in the consumer install vector (Gate-1 r3/F5
ruling — nothing is absorbed out of existence).

What it generates (so the consumer writes none of it): the unplugin transform preset
(enforce-undefined, dev/runtimeInjection by command, throw mode, module resolution), the
F9 layer statement (first head element, lawful utilities-win order), and the dev-HMR
index.html wiring. See index.js header for the full rationale.
