# r13 Tasks

## 1. Dialog 网格化（子代理 A）
- [x] 1.1 ruler + subgrid 三段对齐 + separator 轨道 + border 退役
- [x] 1.2 @container 响应式（inset/宽度/foot）+ no-subgrid 回退
- [x] 1.3 footer `actions` slot（自动 ButtonGroup + ghost 经 context）
- [x] 1.4 dialog spec 更新（结构/回退/响应式断言）+ palette 组合回归

## 2. ButtonGroup 升级（子代理 B）
- [x] 2.1 context 增面：variant 下传 + separator 按需（Separator 实缝）
- [x] 2.2 grid 化（inline-grid + auto-flow column，法则保留）
- [x] 2.3 溢出双模式：wrap（默认）/collapse（DropdownMenu 收纳）
- [x] 2.4 PressButton/IconButton 的组变体消费 + spec 更新

## 3. 整合（ZCode）
- [x] 3.1 镜像/manifest/门禁（specs 全量 + verify:* + build）
- [ ] 3.2 子代理对抗预审 → codex → Owner 终报
