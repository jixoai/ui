# Ring rides the token — uniform weight for every dot variant (Owner r6)

## Why

The Owner's ruling on the r5 ring exemption: "要么移除 ring，要么也吃
token（1px 边）". Chosen: ring EATS the token — removing the variant
would be a breaking API change and would lose the one grammar no other
variant provides (a node that stays hollow even when completed);
uniform weight keeps the family's visual contract exact while the
variant's semantic survives through its remaining identity channels.

## What Changes

- timeline.css: the ring block drops `border-width: 2px` — the base
  dot rule's `border: var(--jx-tl-stroke-w) solid var(--border)`
  applies; the ring's identity is PERMANENT HOLLOWNESS (transparent
  center outranking the completed fill; border COLOR still steps
  with the ladder). Weight is no longer part of any variant's
  identity — the beam preset remains the ONE standing exemption
  (a traveling light, not a connector).
- The standing spine-laws probe (this change's research/) MERGES the
  archived r4 joint-lap probe and the r5 alignment probe into ONE
  receipt, and replaces the variant-hardcoded band depths with LIVE
  computed-border derivation: a perpendicular border width cuts
  √2× along the 45° bevel axis, so the hollow's inner vertex radius
  = r − bw·√2 and the join windows/interior boxes derive from the
  measured border — any token value proves, no variant assumptions.
  New checks: uniform weight (every dot border, ring included ===
  token === structural stroke) + ring-at-token. 10/10 ×3 stable.
- registry.json timeline docs: exemptions sentence updated (beam the
  one standing exemption).
- MINIFY round: no Codex gate; receipts local (batteries,
  build:registry, mirror, strict, the standing probe).
