# review-r1 — the no-codex remix collision (2026-09-07)

Round 1: two designer subagents in parallel (proposals/a — the
typography context; proposals/b — the parts upgrades). Round 2: one
adversarial reviewer attacking both against the live repo at
ec1faad4, verifying every load-bearing claim (14 findings F1–F14,
rulings on all 19 open questions).

Key verified facts: the 11-knob Prose escapes the A1 vocabulary gate
with only `size` hitting (absentSlot satisfies the Defaults law — no
version bump); the specificity ladder arithmetic is exact against the
real selectors; markdown sovereignty holds by cascade; the Heading
var-fallback utility is dedup-safe under the repo's cn config; the
five new data hooks are shadow-clean; `externalLink` is inline-core
sync (SSR-safe); blockquote's list/link markdown assertions survive
the deltas (hooks/attributes only — no class-string assertions).

Key amendments folded: the .svelte.ts double-runtime hazard gates the
context-pair placement (inline default, empirical promotion); the
gradient mechanism writes fill-only, never color-transparent
(the currentcolor trap); print restore lives in the sheet (the paged
clone never re-runs JS providers); the layer statement is
load-bearing spec text; tonal+shadow-1 is a stated near-no-op with
its spec sentence amended; the blockquote delta is component-wide and
the Owner browser review (dev + production, 1-vs-4 side-by-side) is
the change-exit criterion; the docs/registry freeze chain is priced.

Verdicts: both proposals SOUND-WITH-AMENDMENTS; all rulings adopted
into design.md. The integrated design carries the collision's output;
the proposals + this record preserve the reasoning trail.
