# Report 22 — color-picker, REVIEW (1st, marginalia)

agent: marginalia · 2026-09-22 · route
`apps/www/src/routes/docs/components/color-picker.html/` (+page.ts;
curation `src/lib/ui/props-table/docs/color-picker.docs.ts`; generated
meta `src/lib/meta/color-picker.meta.ts`) · coder scribe (integrated
6c3a1c0f) · law: mdn-doc-style §5+§6, emission-form / wall-clock hue trap,
declaring-element, §1 native collision, carriers bijection · reviewer #2:
quill (after — independence held; quill's review not read at filing
time). Every verdict re-derived from source, raw SSR bytes, or live probes
on :5244 (co-resident specimens, single-evaluate; the brand-hue wall-clock
trap honored — the caret is asserted by the DARK FORMULA's L/C signature
plus the −4° drift, never absolute hue).

## Verdict: PASS

**0 BLOCKER · 0 MAJOR · 1 MINOR · 1 NIT.** The stamped-never-consumed
falsification reproduces exactly (grep + grammar + negative paint probe),
the density ruler equation measures to the digit, the theme split's three
classes all verify with the −4° drift measured live, the EXTRA chain's
result is SSR-true, and the matrix re-pin is honest with the
AST-impossibility reason on record. **Closure-ready pending quill's 2nd
review.**

## Findings

1. **MINOR — the family's own props comment still claims "CONSUMED by the
   family" (color-picker.svelte:180-181), contradicted by the page's
   measured SUPPLY-ONLY row, and the drift flag doesn't name it.** The
   page header flags the falsification ("the old page's 'CONSUMES size
   and color' claim was falsified by probe and retires") and the served
   prose never repeats the consumption claim ✓ (the color row is
   SUPPLY-ONLY with the negative-grep + negative-probe receipt; the
   density row's CONSUMED is the honest ambient-scope story — a different
   axis). But the living family comment predates the falsification and
   still teaches the wrong consumption model to every reader of the
   source. Fix: family-owner one-line comment correction + a drift-ledger
   reference (the dispatch's drift-class flag).
2. **NIT — the recorded EXTRA chain's head is not reconstructable from
   the current meta.** The dispatched chain reads "27 meta − 3 citation
   dupes = 24 unique"; the CURRENT color-picker.meta.ts carries **24 raw
   entries with zero duplicate keys** (the three quoted keys 'data-density'/
   'aria-invalid'/'aria-describedby' each appear once). The RESULT
   verifies exactly (24 unique − 8 axis − 4 hidden = 12, SSR-confirmed),
   but "27 raw − 3 collapses" describes a transient count — the three
   quoted keys are each present once. Consolidation note for the archive;
   no page prose makes the 27 claim.

## Verified-TRUE receipts (all my own probes/current tree)

- **Stamped-never-consumed falsification.** My own grep: **0 readers** for
  --jx-size-effective / -shape / -radius / -color / -elevation / -motion /
  -radius-factor / -inset-effective across ui/color-picker/ (5 files).
  Live grammar: the color="error" root's style attr =
  `--jx-color-effective: var(--jx-color-error)` (named → §12 indirection,
  byte-exact); the number-form grammar verified in source
  (defaults.svelte.ts:613-617: `oklch(var(--jx-color-formula-l, 0.6489)
  var(--jx-color-formula-c, 0.237) calc(H + var(--jx-color-formula-drift,
  0)))`) and stamped verbatim on a live clone → **border, well and shadow
  byte-identical to control** (negative probe). The error specimen:
  well/border/shadow **unmoved** vs the default row (negative paint).
- **Density via the ruler equation.** Trigger well (`min-block-size:
  var(--jx-color-lane)`, color-picker.css:25): xs **34px** / sm **36px** /
  default **46px** / lg **58px**; type **11/12/13/15px** — the dispatch's
  exact ladder. Number coefficient 3 → **46/13 unmoved** (coefInert).
  Named rung: data-density="lg" stamps AND the root style shows
  `--jx-density-coefficient: 1` — **the coefficient reset to 1 verified
  on the root style attr**. The equation's 2×--jx-inset term is really
  read (trigger padding-inline var(--jx-inset): 12px default / 16px lg
  measured).
- **Theme split three classes, one-evaluate co-resident specimens.**
  FLIPS: caret `oklch(0.6489 0.237 357)` → `oklch(0.7044 0.1872 353)` —
  the dark formula's **L 0.7044 / C 0.1872 signature with the −4° drift
  measured live** (357−353); swatch border oklch(0 0 0) → oklch(1 0 0);
  well shadow rgba(0,0,0,0.12) 1px 1px inset → **rgba(255,255,255,0.12)
  1px 1px inset** (--shadow-well black→white). FROZEN: the lane's
  border/ink/bg byte-identical across the island (stylex aliases at
  :root). THEME-NEUTRAL: the conic swatch + white/black overlays + #000
  halo are color-space constants (color-picker.css:16-19 exemption
  comments). Prose numeric hygiene: the theme row names tokens only — no
  absolute hue anywhere in the served row (the wall-clock trap honored;
  the only oklch numbers on the page are demo VALUES, not theme claims).
- **EXTRA arithmetic — the result exact.** Meta parsed (string-aware,
  hyphen-inclusive): **24 unique entries**, zero duplicate keys; 8
  axis-named; 4 hidden by curation (id, data-density, class, rest — the
  checkbox pattern, color-picker.docs.ts:79-82) → **12 family rows**,
  SSR-confirmed (value bind, format, name, label, error, disabled,
  variant, showSwatch, showValue, lane, aria-invalid, aria-describedby);
  universal 8; axes 8. The aria rows are the family's OWN interface
  members (the input.svelte validation merge) and rightly stay. The
  "27 − 3" head: see the NIT — the current meta has no duplicate pairs to
  collapse.
- **Matrix re-pin honest.** Fixture: color-picker ×2 entries (density +
  size, both tableIndex 1, marker "scope") with the full provenance note —
  the hand table[0] rows retired, and **variant's own-marker removed with
  the AST-impossibility reason in the fixture note** ("colorPickerSurface
  VariantSlot, the curation restores the bare literal, is not AST-pinnable
  and leaves the matrix"). Ambient solo: **283/283 PASS** (exit 0).
- **Standard**: toc **8/8** ×1 in DOM order; `add color-picker` ×4;
  query two-generic served + flip **1280→58/15 ↔ 600→36/12 ↔ 1280** both
  directions; PILOTS types+axes same-source blocks green (canvas
  same-source solo **79/79**); lab/catalogue/error hand mirrors with the
  rejection class documented (:54-60, the avatar precedent); measurement-
  first header (probed 2026-09-22); declaring-element + PROBE-READINESS +
  raw-SSR discipline (the raw-`>` encoding respected on the query grep);
  test/ pins: color-picker.spec.ts (20 tests), defaults-form-families,
  density-adoption-form-boolean, canvas-same-source, docs-ambient.
- **Tier 2 justified**: old page (6c3a1c0f~1) = 336 lines, 0
  DocsInstall/DocsSeeAlso, and its :324 summary carries the falsified
  "The family CONSUMES size and color" claim — the rewrite retires it
  with the measured falsification; hero catalogue summary derives from
  the registry catalog (no hand copy).

## Gates (re-run this review)

| gate | result | tail |
|---|---|---|
| docs-ambient-vocabulary solo | exit 0 | **283/283 passed** (the re-pin's matrix count) |
| canvas-same-source solo | exit 0 | **79/79 passed** (types+axes PILOTS blocks green) |
| `verify:docs-universal` | exit 0 | `GREEN: 110/110 component pages render the shared universal section (110 markers)` |
| `verify:tailwindless` | exit 0 | receipt verbatim: `files=2 identities=7 occurrences=7 zones={routes:1, site-libs:0, ui:6} forms=42 — bound verbatim` |

## Process evidence

- Port :5244 empty before (lsof exit 1); dev server background task (log
  /tmp/marginalia-22-dev.log), killed by PID at the end with the
  lsof-empty receipt in the final message.
- Probes: /tmp/marginalia-22-{probe,probe2,diag,diag2}.mjs — probe1's
  serialize crash (a function passed as the evaluate argument) and the
  first diagnostic (the inner .jx-field wrappers without fields) are kept:
  the stamped root is the outer `.jx-field` (color-picker.svelte:407-413),
  the well floor and shadow live on the inner trigger span
  (`[data-jx-color-picker-wrap]`, css :25/:182). SSR:
  /tmp/marginalia-22-ssr.html (1,305,801 bytes). Old page:
  /tmp/marginalia-22-old.svelte. Gates: /tmp/marginalia-22-{amb,univ,twl,canvas}.log.
- Independence: reviewer #2 (quill) has not reviewed yet — this is the
  1st review.
- Working tree carries sibling in-flight work (unchecked, untouched). NO
  commits, NO push.
