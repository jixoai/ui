# T140a — SECOND REVIEW markdown.html (quill)

**1st:** vellum 127, PASS 0M/1m/1L/0N, Tier 2 proposed. Owner = marginalia. The CODE
micro (vellum 132) landed the family 47→0 guard pass. **2nd protocol:** her reports
(127 + 132) opened FIRST; the landed state verified at gate + DOM + source layers;
receipts re-derived; fresh axis = my own no-jx-pure census with tag classification.
Fresh build exit 0 at HEAD **8044b180** (== the dispatch floor); probes on the dist via
`vite preview` (deep-section scroll-mount passes included).

## Verdict: PASS — 0 M / 0 m / 0 L / 0 N — Tier 2 confirmed

## Landed items — verified

1. **The family 47 → 0 — HOLDS on my independent run**: ui/markdown = **0 ERRORs** (7
   standing warns) on the batch's ONE saved svelte-check; the guard-pass shape
   (accordion_group head-position, isNodeSequence, the Extract suite, the
   InlineWrapper intersection, the htmlMark cast) is in the served source — and both
   twins remain **byte-identical** (markdown-node.svelte + parse.ts, registry ⇄ www).
   Her "47 is the campaign's largest family gate" is now the campaign's closed gate.
2. **The fleet PlayRow ID law — landed and visible here**: her LOW-1 (the duplicate
   `jx-play-row-1-label` positional ids on two-canvas pages) is GONE — my id census
   finds the `jx-play-row-N-label` family with unique ids page-wide (the $props.id()
   law landed at 4ab37d41 covers this page's canvases).

## Her receipts re-derived — concordant

- **Code → CodeCard (16 instances) + tables → the registry Table (4 `div[data-kind=
  "table"]` wrappers)** — the coverage map's first two rows, re-censused exactly.
- **The security floor**: the html-equivalence seat serves; the family's zero-@html
  posture stands (no contradiction observable from the served side).
- **The streaming seat serves** (the live chunk feed section, the keyed-block
  memoization seam's host).
- **toc == DOM == rail 10/10**, zero dangling; h1 ×1.

## Fresh axis — my own no-jx-pure census, tag-classified

Her count: **42 `.no-jx-pure` elements**. My independent census on the fresh dist:
**45**, classified: H2 ×5 · BLOCKQUOTE ×8 · CODE ×16 · DIV ×5 · FIGURE ×3 · H3 ×4 ·
UL ×3 · HR ×1 — the escape grammar serves across every box-owning block class the
coverage map's "Escape" column names, at a slightly higher count than her pass (the
consolidation window's page growth — the grammar scales with the page, which is the
claim). The count is a census, not a contract; the per-tag spread is the stable shape.

## The task-pair receipt — re-derived with her owned exclusion

Her probe fault #1 is this page's recurring trap: the streaming demo's play/pause
PlayRow checkbox sits inside an `li` and pollutes naive task censuses (7 boxes, 6
disabled). Excluding `#markdown-streaming`: **6/6 task checkboxes disabled + 6/6 task
lis compute list-style none** — the marker-suppression + disabled-inputs pair, exact.
The excluded 7th box is the demo's own control (1, accounted).

## Gates (batch-shared)

| Gate | Result |
|---|---|
| svelte-check (ONE run, /tmp/t140-scheck.log, grepped) | markdown.html: **0 diagnostics** (her :436 cx clone CLOSED); ui/markdown family: **0 ERRORs** — the 47→0 holds on the independent run |
| verify:docs | **rc=0 — fully green** |
| verify:docs-universal | GREEN 110/110 (once per batch) |
| T94 | zero pseudo-class pairs |
| Mirror law | markdown-node.svelte + parse.ts cmp-identical |

## Probe faults owned (mine)

1. The task census repeated her owned fault #1 (7 boxes) before applying her
   li/section-context exclusion — her fault ledger WAS the instrument note; I re-hit it
   anyway and the exclusion is now a permanent line in my probe.
2. Deep sections required the scroll-mount pass before the census (her owned fault #2,
   same shape).

## Process

Port **5241** (batch preview seat): rc=1 before → fresh dist → after gates killed by
PID + wrapper → port **EMPTY** (0 lines), zero orphans. NO commits, NO product-tree
edits. Artifacts: /tmp/t140-md.mjs, md2, -ssr.html, batch logs.
