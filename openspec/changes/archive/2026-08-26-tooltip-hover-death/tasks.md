# tasks — tooltip hover death

- [x] Capture the death lifecycle in a real browser (boundary events +
      toggle, relatedTarget trail) — the stale-box transient + the
      missing compensating enter isolated
- [x] r1 close-seam flags + fire-time recheck (superseded mechanism,
      retained as lock 2)
- [x] r3 hit-transparency before promotion + geometric verdict with
      halo re-arm; touch coordinates excluded from the verdict
- [x] r5 filter channel pinned `none` at the A→B seam, rest pose, and
      reduced-motion rest (the Owner's 10× slow-mo catch)
- [x] r6 materials formula-owned: `--jx-p: 1` parked inline from entry
      start; glass/veil read at true p=1 values
- [x] r7 rest releases every material pin — theme switch while resting
      re-flows live (verified); only filter:none + SHADOW() stay pinned
- [x] jsdom tests: real sibling-switch order (out→leave→over→enter),
      touch-liftoff regression; batch2 Tooltip section green
- [x] Dual-source copies byte-identical; payload parity GREEN
