# Gate-1 r2 verdict — visual-g1 (Codex, gpt-5.6-terra xhigh), 2026-09-15

**NEEDS-WORK 6.8/10** (worked 34m 26s; r1 was 5.8). r1 closure check: five
of six confirmed closed (mermaid six-scenario block SHA-256 equal both
sides, 7714a5e5…; subtractive veil, grid dialect, observer contract, and
label/fill/border contrast all verified against the living specs). Four
blockers remained:

1. **W3's exemption retirement had no css-architecture delta** — the
   living law still names the timeline beam (TRANSIENT INK) and the
   scroll-progress absolute channel (CONTAINING-BLOCK NEEDS);
   component-delta claim contradicts the living spec.
2. **W2's connector in the 3:1 set but absent from sampling/tasks** —
   the acceptance objects and the task only listed label/fill/border.
3. **W4 delta vs design drift** — delta named only CORE + hand-drawn
   adapter (missing the native CAPABILITY STYLES third part) and merged
   region-focus-within + thumb-focus into one "focus" pin while claiming
   three separate probes.
4. **No mirror/payload acceptance in tasks** — registry changes must sync
   the apps/www mirror + manifest and prove source-to-mirror and
   source-to-payload invariants (mirror-sync spec); tasks only said
   registry/docs/clean-consumer.

Non-blocking: a missing blockquote `>` at the mermaid rationale append;
"unsccoped" typo; W1 walk-up boundary tests (alpha, reparent); W3
aria-hidden on the decorative SVG + DOM snapshot for the structural
landing points.

## r3 response (this commit)

- B1: NEW `specs/css-architecture/spec.md` delta — the
  grid-supplies-stacking requirement MODIFIED by programmatic splice from
  the living spec with EXACTLY the two example retirements (each carrying
  a dated breadcrumb noting the retirement); diff-verified: 13 changed
  lines all inside the two bullets, scenarios + title byte-equal.
- B2: connector joins the sampled objects everywhere — delta backdrop
  scenario ("connector strokes ≥ 3:1 against the veil ground sampled
  adjacent to each object — 2px past the node border, and at the midpoint
  of the longest edge connector"), design W2 objects list, task 2.2.
- B3: delta names the THREE kit parts (CORE / HAND-DRAWN INTERACTION
  ADAPTER / NATIVE CAPABILITY STYLES, each consumed by exactly its own
  component) and the pin scenario now enumerates FOUR separately
  probe-asserted pins (region focus-within, thumb focus, drag, hover —
  each tested in isolation).
- B4: task 5.4 added (mirror sync + manifest + verify:mirror + shadcn
  build/payload parity + verify:deps + the two real-install cases), the
  standing-gates paragraph requires per-task mirror sync in the same
  commit, and proposal Impact carries the mirror/payload paragraph.
- Non-blocking all adopted: blockquote restored as a new `>` line (the
  rationale's living-byte prefix property re-verified True); typo fixed;
  W1 task 1.2 adds alpha-skip + reparent re-walk boundary tests; W3 task
  3.1 adds `aria-hidden="true"` on the decorative SVG + a DOM-snapshot
  test locking the grid host / `<ol role="list">` / rest-attributes
  landing points.
