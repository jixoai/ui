// icon — pure barrel (icon-component-pipeline B1): default = the
// canonical main; export * carries module-level named exports/types
// (none today — the component is the item's whole surface). No logic
// lives here.
export { default } from './icon.svelte';
export * from './icon.svelte';
