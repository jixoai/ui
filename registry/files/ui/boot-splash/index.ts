// boot-splash — pure barrel (the separator law): default = the
// canonical main; export * carries module-level exports/types
export { default } from './boot-splash.svelte';
export * from './boot-splash.svelte';
