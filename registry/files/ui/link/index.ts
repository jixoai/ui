// link — pure barrel: default = the canonical main (a native <a> with
// the face's non-nav link paint as its own utilities); export *
// carries module-level named exports/types. No defaults file (no
// public style prop — the header comment records the ruling). No
// logic lives here.
export { default } from './link.svelte';
export * from './link.svelte';
