// math-inline — pure barrel (the toc/inline-code precedent): default =
// the one surface; export * carries module-level named exports/types.
// No logic lives here.
export { default } from './math-inline.svelte';
export * from './math-inline.svelte';
