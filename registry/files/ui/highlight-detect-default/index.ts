// highlight-detect-default — pure barrel (the icon/index.ts shape): the
// default export is the canonical main; `export *` carries module-level
// named exports (none today — the wrapper component is the item's whole
// surface). No logic lives here.
export { default } from './highlight-detect-default.svelte';
export * from './highlight-detect-default.svelte';
